---
phase: 04-beamer-slide-deck
plan: 02
type: execute
wave: 2
depends_on:
  - "04-01"
files_modified:
  - research/slides/presentation.tex
autonomous: true
requirements:
  - BEAM-03
  - BEAM-04
  - BEAM-05

must_haves:
  truths:
    - "Solution frames show architecture context diagram and sequence diagram as included PNGs"
    - "Demo frame includes the exact forge command for the integration test and 3-5 observation bullets"
    - "Roadmap frame has two-column Built vs Next layout with ambitious closing tone"
    - "The complete .tex file compiles with pdflatex without errors"
  artifacts:
    - path: "research/slides/presentation.tex"
      provides: "Complete 10-frame Beamer presentation"
      contains: "\\\\end{document}"
  key_links:
    - from: "research/slides/presentation.tex"
      to: "docs/diagrams/context-diagram.png"
      via: "\\includegraphics in solution frame 1"
      pattern: "includegraphics.*context-diagram"
    - from: "research/slides/presentation.tex"
      to: "docs/diagrams/sequence-diagram.png"
      via: "\\includegraphics in solution frame 2"
      pattern: "includegraphics.*sequence-diagram"
---

<objective>
Complete the Beamer presentation by appending solution frames (2), demo frame (1), and roadmap frame (1), then close the document and verify compilation.

Purpose: Deliver the complete 10-frame deck with architecture diagrams, demo instructions, and roadmap -- making the presentation ready for PDF generation.
Output: research/slides/presentation.tex (complete, compilable)
</objective>

<execution_context>
@/home/jmsbpp/.claude/get-shit-done/workflows/execute-plan.md
@/home/jmsbpp/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-beamer-slide-deck/04-CONTEXT.md
@.planning/phases/04-beamer-slide-deck/04-01-SUMMARY.md

@research/slides/presentation.tex
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add solution, demo, and roadmap frames</name>
  <files>research/slides/presentation.tex</files>
  <read_first>
    - research/slides/presentation.tex
    - .planning/phases/04-beamer-slide-deck/04-CONTEXT.md
  </read_first>
  <action>
Open research/slides/presentation.tex and append the remaining 4 frames before closing the document.

**Solution Frame 1: "Architecture" (or similar title)**
- Include context diagram: \includegraphics[width=\textwidth]{../../docs/diagrams/context-diagram.png}
  (path from research/slides/ -> ../../docs/diagrams/)
- Brief caption or annotation: FCI Hook at center, Protocol Adapters (V3/V4) feeding events, Reactive Network bridging cross-chain, Vault + CFMM downstream
- Minimal text -- let the diagram speak

**Solution Frame 2: "Cross-Protocol Modularity" (or similar title)**
- Include sequence diagram: \includegraphics[height=0.8\textheight]{../../docs/diagrams/sequence-diagram.png}
  (this diagram is tall -- 1358x2196 -- use height constraint, not width)
- Annotation: "listenPool() -> swap/mint/burn events -> metric update -> DeltaPlus derivation"
- Emphasize: same FCI oracle, multiple protocol adapters, reactive network bridges events cross-chain

**Demo Frame: "Live Demo"**
- Forge command in a verbatim/lstlisting block:
  forge test --match-path "test/fee-concentration-index-v2/protocols/uniswapV4/NativeV4FeeConcentrationIndex.integration.t.sol" -vv
- 3-5 bullets on what to observe:
  1. Swap updates $\AT$ (fee concentration index tracks each trade)
  2. Mint increments $\PosCount$ (new positions enter the pool)
  3. Burn triggers $\DeltaPlus$ derivation (exit computes concentration deviation)
  4. All metrics computed on-chain in a single V4 hook callback
- Use \texttt{} for contract/function names

**Roadmap Frame: "What's Next"**
- Two-column layout using Beamer columns environment:
  Left column "Built":
  - FCI oracle live on Uniswap V4
  - Reactive adapter bridging V3 events cross-chain
  - Backtest validation: 18.8% of positions better off hedged, +\$23 mean hedge value
  Right column "Next":
  - Linearized power-squared CFMM (trading function complete in spec)
  - Vault settlement mechanism
- Closing line (ambitious tone, centered below columns):
  "The mathematical specification is complete, the oracle tracks metrics across protocols, and the next milestone delivers the full insurance product."

**Close document:**
\end{document}
  </action>
  <verify>
    <automated>cd /home/jmsbpp/apps/ThetaSwap/thetaSwap-core-dev && grep -c "\\\\begin{frame}" research/slides/presentation.tex | grep -qE "^(9|10|11)" && grep -q "context-diagram" research/slides/presentation.tex && grep -q "sequence-diagram" research/slides/presentation.tex && grep -q "forge test" research/slides/presentation.tex && grep -q "end{document}" research/slides/presentation.tex && echo "PASS" || echo "FAIL"</automated>
  </verify>
  <acceptance_criteria>
    - grep -c "\\begin{frame}" research/slides/presentation.tex returns 10 (or 9-11 if optional closing frame)
    - grep "context-diagram" research/slides/presentation.tex returns match
    - grep "sequence-diagram" research/slides/presentation.tex returns match
    - grep "forge test" research/slides/presentation.tex returns match
    - grep "18.8" research/slides/presentation.tex returns match (backtest stat in roadmap)
    - grep "end{document}" research/slides/presentation.tex returns match
    - grep "columns" research/slides/presentation.tex returns match (two-column roadmap)
  </acceptance_criteria>
  <done>All 10 frames present: title + 2 problem + 3 evidence + 2 solution + 1 demo + 1 roadmap. Document closed with \end{document}.</done>
</task>

<task type="auto">
  <name>Task 2: Verify pdflatex compilation</name>
  <files>research/slides/presentation.tex</files>
  <read_first>
    - research/slides/presentation.tex
  </read_first>
  <action>
Run pdflatex compilation from the research/slides/ directory:

  cd research/slides && pdflatex -interaction=nonstopmode presentation.tex

Check the output for errors (not warnings -- Beamer always produces warnings).

If compilation fails:
1. Read the .log file to identify the error
2. Fix the .tex file (common issues: missing packages, bad image paths, unescaped characters)
3. Re-run pdflatex

Run a second pass if needed (for references/ToC, though this flat deck likely needs only one pass).

Verify that research/slides/presentation.pdf exists and has approximately 10 pages.

If pdflatex is not available, check for latex or xelatex. Use whichever is present. If none available, document the issue -- the .tex file is still the deliverable.
  </action>
  <verify>
    <automated>cd /home/jmsbpp/apps/ThetaSwap/thetaSwap-core-dev/research/slides && pdflatex -interaction=nonstopmode presentation.tex 2>&1 | tail -5 && test -f presentation.pdf && echo "PDF exists" || echo "PDF missing"</automated>
  </verify>
  <acceptance_criteria>
    - pdflatex exits without error (warnings OK)
    - research/slides/presentation.pdf exists
    - PDF has approximately 10 pages (verify with: pdfinfo presentation.pdf | grep Pages if pdfinfo available, otherwise file size > 50KB as proxy)
  </acceptance_criteria>
  <done>presentation.tex compiles to presentation.pdf without errors. PDF contains all 10 frames.</done>
</task>

</tasks>

<verification>
- research/slides/presentation.tex is complete with \end{document}
- Contains 10 \begin{frame} blocks total
- Solution frames include both architecture PNGs via \includegraphics
- Demo frame has the forge test command and observation bullets
- Roadmap frame has two-column Built vs Next with backtest stats
- File compiles with pdflatex to produce presentation.pdf
</verification>

<success_criteria>
1. Solution frames show real architecture (included diagrams from docs/diagrams/), not vaporware
2. Demo frame gives the audience everything needed to run the test themselves
3. Roadmap frame leaves the impression "they've done the hard part" -- research + oracle built, CFMM + vault are next steps not blockers
4. The .tex file compiles to PDF without errors
5. Complete deck: 10 frames, 5-7 minute pacing, one idea per frame
</success_criteria>

<output>
After completion, create `.planning/phases/04-beamer-slide-deck/04-02-SUMMARY.md`
</output>
