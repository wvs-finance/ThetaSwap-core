---
phase: 03-repository-artifacts
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - research/README.md
autonomous: true
requirements: [RREAD-01, RREAD-02, RREAD-03]

must_haves:
  truths:
    - "Research README provides a detailed summary covering problem, methodology, and key findings"
    - "Research README has direct links to actual artifacts: notebooks, modules, LaTeX files, data fixtures"
    - "Research README is organized by four domains: Econometrics, Backtest, Model, Data"
    - "Key equations appear in collapsible details blocks with GitHub LaTeX math rendering"
  artifacts:
    - path: "research/README.md"
      provides: "Detailed research artifact index"
      contains: "## Econometrics"
  key_links:
    - from: "research/README.md"
      to: "research/econometrics/"
      via: "relative markdown links to modules"
      pattern: "econometrics/"
    - from: "research/README.md"
      to: "research/backtest/"
      via: "relative markdown links to modules"
      pattern: "backtest/"
    - from: "research/README.md"
      to: "research/model/main.tex"
      via: "relative markdown link"
      pattern: "model/main\\.tex"
    - from: "research/README.md"
      to: "research/notebooks/"
      via: "relative markdown links to notebooks"
      pattern: "notebooks/"
---

<objective>
Create research/README.md as a detailed table-of-contents-style guide to all research artifacts, organized by domain (Econometrics, Backtest, Model, Data) with key equations in collapsible blocks and direct links to every artifact.

Purpose: Someone who wants to dig into the research should find every artifact organized by domain without hunting through directories.
Output: research/README.md (new file)
</objective>

<execution_context>
@/home/jmsbpp/.claude/get-shit-done/workflows/execute-plan.md
@/home/jmsbpp/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-repository-artifacts/03-CONTEXT.md
@.planning/phases/01-problem-research-narrative/01-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write research/README.md</name>
  <files>research/README.md</files>
  <read_first>
    - research/narrative/problem.md (for the problem summary intro)
    - research/narrative/research-summary.md (for key findings, equations, statistics to reference)
    - research/model/main.tex (first 50 lines -- confirm structure, title, key sections to point to)
    - research/model/econometrics.tex (first 30 lines -- confirm it exists and section structure)
    - research/model/payoff.tex (first 30 lines -- confirm FCI definition location)
    - research/econometrics/ (list directory to enumerate all 13 modules)
    - research/backtest/ (list directory to enumerate all 8 modules)
    - research/data/ (list directory to enumerate subdirectories: fixtures, queries, scripts)
    - research/notebooks/ (list directory to enumerate all 4 notebooks)
    - research/figures/ (list to confirm 4 PNGs)
    - research/tests/ (list to confirm test suite exists)
  </read_first>
  <action>
Create research/README.md with the following structure:

**Header: `# ThetaSwap Research`**
One-line intro: "Empirical research supporting the Fee Concentration Index -- a quadratic deviation exit hazard model for adverse competition risk in concentrated liquidity AMMs."

Brief overview paragraph (2-3 sentences): Summarize what the research proves -- inverted-U relationship between fee share deviation and exit hazard, turning point at delta* ~ 0.09, demand identification via 2.65x real vs null A_T, 41-day observation window over 600 positions. Point to narrative/problem.md and narrative/research-summary.md for the full story.

**Section: `## Econometrics` (RREAD-03)**
One-line summary: "Duration and hazard models estimating how fee concentration deviation drives LP exit rates."
List key modules from research/econometrics/ with one-line descriptions. Include at minimum:
- types.py (data structures for positions, durations, hazard records)
- data.py (data loading and transformation)
- ingest.py / ingest_uniswap.py (Uniswap event ingestion)
- hazard.py (hazard model implementation)
- duration.py (duration analysis)

Link to notebooks that use these: notebooks/eth-usdc-insurance-demand-identification.ipynb

Collapsible `<details>` block with key equation:
- A_T (fee concentration accumulator): $A_T = \frac{1}{N} \sum_{k=1}^{N} x_k^2 \cdot B_k$
- Turning point formula: $\delta^* = -\hat{\beta}_1 / (2\hat{\beta}_2)$

**Section: `## Backtest` (RREAD-03)**
One-line summary: "Insurance backtest engine simulating trigger-based and threshold-based insurance payoffs against historical data."
List key modules from research/backtest/ with one-line descriptions. Include at minimum:
- daily.py (daily position tracking)
- sweep.py (parameter sweep for trigger calibration)
- calibrate.py (model calibration)
- payoff.py (insurance payoff computation)
- plotting.py (publication-quality figure generation)

Link to notebooks: notebooks/eth-usdc-backtest.ipynb

Collapsible `<details>` block with key equation:
- DeltaPlus: $\Delta^+ = \max(0, 1 - A_T)$

**Section: `## Model` (RREAD-03)**
One-line summary: "LaTeX mathematical specification of the FCI mechanism, payoff structure, and econometric methodology."
List key files:
- model/main.tex (master document)
- model/main.pdf (compiled specification)
- model/econometrics.tex (full econometric specification with regression tables)
- model/payoff.tex (FCI definition, co-primary state, price mapping)

**Section: `## Data` (RREAD-03)**
One-line summary: "Cached Dune query results, frozen fixtures for reproducible fork tests, and oracle scripts."
List subdirectories:
- data/fixtures/ (frozen datasets for Forge fork tests via vm.readFile)
- data/queries/ (Dune SQL queries)
- data/scripts/ (FFI oracle scripts -- fci_oracle.py, hhi_oracle.py)
- data/raw_events/ (raw Uniswap event data, if present)

**Section: `## Figures`**
One-line: "Publication-style plots generated from Phase 1 notebooks."
List the 4 PNGs: alpha-sweep.png, dose-response.png, reserve-dynamics.png, trigger-days.png

**Section: `## Tests`**
One-line: "Python test suite (114 tests) covering econometrics, backtest, and data modules."
Command: `PYTHONPATH=research pytest research/tests -v`

All paths in the README must be relative to research/ (since the file lives there). Use `./econometrics/` not `research/econometrics/`.
  </action>
  <verify>
    <automated>grep -c "## Econometrics" research/README.md && grep -c "## Backtest" research/README.md && grep -c "## Model" research/README.md && grep -c "## Data" research/README.md && grep -c "<details>" research/README.md && grep -c "notebooks/" research/README.md</automated>
  </verify>
  <acceptance_criteria>
    - grep "## Econometrics" research/README.md returns 1 match
    - grep "## Backtest" research/README.md returns 1 match
    - grep "## Model" research/README.md returns 1 match
    - grep "## Data" research/README.md returns 1 match
    - grep -c "<details>" research/README.md returns at least 2 (collapsible equation blocks)
    - grep "notebooks/" research/README.md returns at least 2 matches (links to actual notebooks)
    - grep "model/main.tex" research/README.md returns at least 1 match
    - grep "econometrics/" research/README.md returns at least 1 match
    - grep "backtest/" research/README.md returns at least 1 match
    - grep "data/" research/README.md returns at least 1 match
    - grep "A_T" research/README.md returns at least 1 match (key equation referenced)
  </acceptance_criteria>
  <done>research/README.md exists with four domain sections (Econometrics, Backtest, Model, Data), collapsible equation blocks, and direct links to all research artifacts. A developer can find any research artifact by domain without directory browsing.</done>
</task>

</tasks>

<verification>
- All four domain sections present with artifact links
- Collapsible details blocks contain key equations with GitHub LaTeX math syntax
- All relative paths are correct (relative to research/ directory)
- Notebooks, modules, LaTeX files, and data directories all linked
</verification>

<success_criteria>
- research/README.md has four domain sections in order: Econometrics, Backtest, Model, Data
- At least 2 collapsible equation blocks with LaTeX math
- Direct links to notebooks, econometrics modules, backtest modules, model LaTeX, data fixtures
- File serves as a complete table of contents for the research directory
</success_criteria>

<output>
After completion, create `.planning/phases/03-repository-artifacts/03-02-SUMMARY.md`
</output>
