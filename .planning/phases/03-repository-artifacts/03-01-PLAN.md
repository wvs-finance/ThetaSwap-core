---
phase: 03-repository-artifacts
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - README.md
autonomous: true
requirements: [ROOT-01, ROOT-02, ROOT-03, DEMO-01, DEMO-02]

must_haves:
  truths:
    - "Root README opens with a single-paragraph project description explaining adverse competition oracle"
    - "Architecture section embeds both mermaid diagrams (context + sequence) inline and they render on GitHub"
    - "Demo section contains the forge command and 3-5 bullets explaining what each scenario demonstrates"
    - "Directory pointers link to research/, src/, test/, docs/ with one-line descriptions"
    - "Section order is Overview -> Architecture -> Demo -> Directory"
  artifacts:
    - path: "README.md"
      provides: "Root project landing page"
      contains: "## Architecture"
    - path: "README.md"
      provides: "Demo documentation"
      contains: "NativeV4FeeConcentrationIndex.integration.t.sol"
  key_links:
    - from: "README.md"
      to: "research/README.md"
      via: "markdown link in overview or directory section"
      pattern: "\\[.*\\]\\(research/README\\.md\\)"
    - from: "README.md"
      to: "docs/diagrams/context-diagram.md"
      via: "mermaid block embedded inline (copied from source)"
      pattern: "```mermaid"
---

<objective>
Create the root README.md as the project landing page with four sections: one-paragraph overview, architecture diagrams (both mermaid blocks embedded inline), demo instructions (forge command + bullet explanation), and directory pointers.

Purpose: The root README is the first thing anyone sees when they find the repo. It must communicate what ThetaSwap is, show the architecture visually, tell them how to run the demo, and point them to details.
Output: README.md (overwritten from current version)
</objective>

<execution_context>
@/home/jmsbpp/.claude/get-shit-done/workflows/execute-plan.md
@/home/jmsbpp/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-repository-artifacts/03-CONTEXT.md
@.planning/phases/01-problem-research-narrative/01-01-SUMMARY.md
@.planning/phases/02-architecture-diagrams/02-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write root README.md</name>
  <files>README.md</files>
  <read_first>
    - README.md (existing content -- preserve logo header block, update everything else)
    - research/narrative/problem.md (condense Section 1 into one paragraph for overview)
    - docs/diagrams/context-diagram.md (copy the full mermaid fenced code block for Architecture section)
    - docs/diagrams/sequence-diagram.md (copy the full mermaid fenced code block for Architecture section)
    - test/fee-concentration-index-v2/protocols/uniswapV4/NativeV4FeeConcentrationIndex.integration.t.sol (skim first 30 lines to confirm test name and scenarios for demo bullets)
    - CLAUDE.md (Project Structure section for directory pointer descriptions)
  </read_first>
  <action>
Overwrite README.md with the following structure. Preserve the existing logo/hero HTML block at the top (lines 1-17 of current file). Replace everything after the horizontal rule with:

**Section 1: Overview (one paragraph)**
Condense the opening of research/narrative/problem.md Section 1 into a single paragraph. Core message: ThetaSwap builds the first on-chain adverse competition oracle -- fee concentration is a distinct LP risk orthogonal to LVR/impermanent loss, and ThetaSwap's FCI Hook tracks it across protocols (V3, V4) to enable hedging via insurance products. End with a pointer: "See [research/README.md](research/README.md) for the full empirical evidence."

**Section 2: Architecture (`## Architecture`)**
Brief intro sentence: "FCI Hook is a protocol-agnostic orchestrator that dispatches behavioral calls via delegatecall to registered protocol facets."

Then embed BOTH mermaid diagrams inline:
1. Sub-header `### System Context` -- copy the full ```mermaid ... ``` block from docs/diagrams/context-diagram.md (lines 7-63). Add the legend note: "Solid border = live on testnet. Dashed border = planned."
2. Sub-header `### Pool Listening Flow` -- copy the full ```mermaid ... ``` block from docs/diagrams/sequence-diagram.md (lines 5-94). Add brief note: "Mint and burn follow the same delegatecall dispatch pattern."

**Section 3: Demo (`## Quick Start / Demo`)**
Content:
```
forge test --match-path "test/fee-concentration-index-v2/protocols/uniswapV4/NativeV4FeeConcentrationIndex.integration.t.sol" -vv
```
Then 3-5 bullets explaining what the test demonstrates:
- Swap scenario: each swap updates the A_T accumulator by computing fee share ratios across overlapping positions
- Mint scenario: adding liquidity increments position count N and registers the position for FCI tracking
- Burn scenario: removing liquidity triggers fee share computation (xk), accumulates the FCI state term, and derives DeltaPlus
- DeltaPlus query: external readers call getDeltaPlus() to get the insurance-relevant oracle value (max(0, 1 - A_T))
- Cross-protocol: the same orchestrator logic works for both V4 native hooks and V3 via reactive network callbacks

**Section 4: Directory (`## Repository Structure`)**
Table or indented list with one-line descriptions:
- `src/` -- Solidity contracts: FCI V2 orchestrator, protocol facets (V4 native, V3 reactive), vault, libraries
- `test/` -- Forge test suites: unit, fuzz, fork, and integration tests
- `research/` -- Econometric analysis, backtest engine, mathematical model, data fixtures ([detailed README](research/README.md))
- `docs/` -- Architecture diagrams (mermaid), implementation plans
- `specs/` -- Contract specifications (per-feature)

Do NOT include: Prerequisites section, make commands, Quick Start clone instructions, Python section. The old README's operational content is replaced by the landing-page style. Keep it brief and high-level per ROOT-03.
  </action>
  <verify>
    <automated>grep -c "## Architecture" README.md && grep -c "```mermaid" README.md && grep -c "NativeV4FeeConcentrationIndex.integration.t.sol" README.md && grep -c "research/README.md" README.md</automated>
  </verify>
  <acceptance_criteria>
    - grep "## Architecture" README.md returns at least 1 match
    - grep -c '```mermaid' README.md returns exactly 2 (context diagram + sequence diagram)
    - grep "NativeV4FeeConcentrationIndex.integration.t.sol" README.md returns at least 1 match (demo command)
    - grep "research/README.md" README.md returns at least 1 match (cross-link)
    - grep "## Quick Start" README.md OR grep "## Demo" README.md returns at least 1 match
    - grep "## Repository Structure" README.md returns at least 1 match
    - grep "DeltaPlus" README.md returns at least 1 match (demo bullet)
    - The file has fewer than 200 lines (brief, not dense -- ROOT-03)
  </acceptance_criteria>
  <done>Root README has overview paragraph, both mermaid diagrams embedded and rendering, demo forge command with scenario bullets, and directory pointers with link to research/README.md. Accessible to mixed audience -- brief and high-level.</done>
</task>

</tasks>

<verification>
- Both mermaid blocks render when viewed on GitHub (fenced code blocks with ```mermaid)
- Demo forge command is copy-pasteable and points to the correct test file
- Link to research/README.md is valid relative path
- Section order: Overview -> Architecture -> Demo -> Directory
</verification>

<success_criteria>
- README.md contains all four sections in correct order
- Two mermaid diagrams embedded inline
- Demo section has forge command + 3-5 explanatory bullets
- Directory section links to research/README.md
- File is under 200 lines (brief, accessible)
</success_criteria>

<output>
After completion, create `.planning/phases/03-repository-artifacts/03-01-SUMMARY.md`
</output>
