---
spec_sha256: 7c72292516f58f3cf2d16464d4f84c3e7d7270ad2c5d3d8ed8fef6b3b2751f5a
spec_version_pinned: 1.0.2 (CORRECTIONS-κ post-Task-1.1 inline integration + Phase-4→Phase-3 phase-numbering typo fix at 5 occurrences; v1.0 sha 72ebe2561a… → v1.0.1 sha 456ba39e18… → v1.0.2 sha 7c72292516… per Task 1.1 PASS-WITH-FLAG disposition memo Option A + RC FLAG-2 closure)
prior_spec_sha256_v1_0_1: 456ba39e188d00bb17471359a5803d6aa8a40de3b3788f17294bab828a968204  # superseded 2026-05-05 by v1.0.2 CORRECTIONS-κ integration
sibling_plan_precedent: contracts/docs/superpowers/plans/2026-04-27-simple-beta-pair-d-implementation.md (Pair D simple-β v2.4; CORRECTIONS-α' Option-α' + Phase-2 CORRECTIONS-β script-form-vs-trio-checkpoint deviation remediation; CLAUDE.md "Pair D Phase 2 PASS verdict" memory)
plan_verifier_v1_wave1: ACCEPT_WITH_FLAGS (Reality Checker, 2026-05-05; 0 BLOCKs / 4 FLAGs / 6 NITs at /tmp/wave1_rc_dev_ai_plan.md; plan-emission unblocked)
plan_verifier_v1_wave2: PASS-WITH-REVISIONS (Senior Project Manager, 2026-05-05; 6 BLOCKs / 5 HIGH / 8 MEDIUM / 5 LOW at /tmp/wave2_spm_dev_ai_plan.md; D1-D19 integrated in v1.1)
plan_verifier_v1_1_wave1: CLOSURE_ACCEPT (Reality Checker, 2026-05-05; all 19 SPM defects + 4 RC FLAGs CLOSED; verdict at /tmp/wave1_rc_dev_ai_plan_v1_1_closure.md; Phase 0 Task 0.1 dispatch UNBLOCKED)
ea_install_path_pin: option_iii_manual_framework_application (user pick 2026-05-05 via β-pivot from disposition memo `contracts/.scratch/2026-05-05-economist-analyst-skill-install-decision.md`; Phase 2.5 Tasks 2.5.1-2.5.4 invoke synthesis memo §3 four-touchpoint framework verbatim; Doc-Verify trailer `orchestrator-applied-EA-framework`)
corrections_kappa_disposition_pin: Option A — Proceed-with-FLAG-disclosure (user pick 2026-05-05 from Task 1.1 PASS-WITH-FLAG disposition memo `contracts/.scratch/dev-ai-stage-1/disposition-memo-task-1.1-flag-A-flag-B.md`); spec v1.0.1 → v1.0.2 inline integration of CORRECTIONS-κ block (FLAG-A cell_count [94, 267] vs Y-feasibility-memo [700, 1200] 5-7× below; FLAG-B raw_share [0.014, 0.031] vs spec §5.1 [0.04, 0.10] 1.3-3× below + logit-derivative [33, 73] vs spec-anticipated 2.34× baseline 3-7× larger amplification); §6 R1 + R3 escalation discipline tightened (sign-different β̂_composite from primary on either R1 OR R3 → §3.3 Clause-A FIRES, no judgment-call escape); Phase-3 result-memo new §11.X disclosure section pre-pinned per spec §9.17 v1.0.2; Task 1.3 dispatch UNBLOCKED under Option A (no smoothing, no Y_p swap, no widening, no re-architect — discipline-respecting path per `feedback_pathological_halt_anti_fishing_checkpoint`)
spec_sha256_v1_0_2: 7c72292516f58f3cf2d16464d4f84c3e7d7270ad2c5d3d8ed8fef6b3b2751f5a  # decision_hash sentinel-intact protocol; on-disk file sha (post-pin) is d90f6302f9473aa938521ed0b7a9b58dc1c849cd74476cd90424f59f3bd3f37e
revision_history:
  - v1.0 2026-05-05 initial draft (Foreground Orchestrator via writing-plans skill; chunked-authoring discipline per `feedback_background_agent_stream_watchdog_timeout` ≤200-line Edit calls; inherits Pair D v2.4 plan format; NEW vs Pair D: Phase 2.5 EA integration per `feedback_economist_analyst_post_analytics_role` 2026-05-04 memory pin, Phase 3 §10 compositional-accounting per spec §9.16 v1.0.1, Section-J cell-size HALT path per spec §6 v1.0.1, X-back-extension into 2014 per spec §4 v1.0.1)
  - v1.1 2026-05-05 inline integration of RC Wave-1 (4 FLAGs) + SPM Wave-2 (6 BLOCKs / 5 HIGH / 8 MEDIUM = D1-D19) defects. Convergent-fix: D10 + RC-FLAG-1 (sha256 non-destructive temp-copy verification protocol); D3 + RC-FLAG-2 (ESCALATE-FAIL Trend Researcher mechanism-failure-attribution dispatch); D11 + RC-FLAG-4 (free-tier compliance pin §9.14); D16 + D24 (Doc-Verify trailer + origin remote). NEW: Phase 0 Task 0.2 EA skill installation gate (D4); Phase 2.5 decomposition into 4 sub-tasks (D9 Tasks 2.5.1/2/3/4); Phase 4 Task 4.1 Step 5 closure-archival per Pair D v2 RC D6 precedent (D2). Timeline revised 2-3 weeks → 2.5-3 weeks unconditional (D1). LOW defects D20-D24 deferred to v1.2 polish cycle.
  - v1.1.1 2026-05-05 CORRECTIONS-κ disposition pin (Option A user pick post-Task-1.1 PASS-WITH-FLAG); single-line frontmatter addition of `corrections_kappa_disposition_pin` field + placeholder for `spec_sha256_v1_0_2`; Task 1.1 reported PASS-WITH-FLAG (cell_count [94, 267] / raw_share [0.014, 0.031] / logit-derivative [33, 73]) with 0 typed exceptions and 0 HALTs; Task 1.2 reported PASS (135-row COP/USD lag panel); Task 1.3 dispatch UNBLOCKED under Option A; CORRECTIONS-κ binds Phase 2 verdict-interpretation through the spec v1.0.2 §11.X disclosure pre-pin (R1 + R3 sign-AGREE-or-ESCALATE under tightened §6 condition).
notebook_trailer_convention: This plan inherits Pair D's deferred-trailer convention for notebook commits during Phase 2. Notebook commits are intentionally trailer-light pre-Phase-3; the 3-way review in Phase-3 audits notebooks retrospectively. Standard 2-wave + 3-way trailers apply at all spec / memo / CLAUDE.md commits.
---

# Simple-β Dev-AI-Cost Iteration — Stage-1 Empirical Validation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use `superpowers:subagent-driven-development` to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking. Per project convention `feedback_no_code_in_specs_or_plans`, this plan is code-agnostic; implementation specifics are deferred to executor sub-agents per task. Per `feedback_specialized_agents_per_task`, each task names the specialist owner.

**Goal:** Empirically test whether Colombian young-worker (14-28) Section J (Información y Comunicaciones) employment share responds positively to lagged COP/USD devaluation, producing a falsifiable feasibility verdict (β > 0 PASS / β = 0 FAIL → escalate to convex-payoff evidence) that determines whether the dev-AI-cost iteration **becomes a candidate for Stage 2 (M sketch on Panoptic) authoring as a separate downstream plan**, or is killed in favor of revisiting the Y feasibility memo's sensitivity arms (Y_s1 DANE EMS / Y_s2 GEIH Section M / Y_s3 UNCTAD EBOPS-9 panel) or sibling-iteration alternatives. Stage 2 + Stage 3 are explicitly NOT promised by this Stage-1 plan.

**Architecture:** Pre-registered single OLS (logit-OLS for bounded Y) regression mirroring Pair D scaffold (sha `964c62cca…ef659`, PASS verdict 2026-04-28 at β=+0.137 p=1.46e-08) with **Y narrowed from broad-services Section G-T to Section J (Information & Communications) specifically**. Y = Colombian young-worker (14-28) Section J employment share (DANE GEIH monthly micro-data, FEX_C_2018 expansion factors, RAMA4D_R4 4-digit codes mapped to Section J via DANE Section table; logit-transformed for bounded [0.04, 0.10] range; 135 monthly observations 2015-01 → 2026-03 inheriting Pair D Option-α' window). X = COP/USD lagged 6-12 months (reuses Pair D X variable; Banrep TRM end-of-month spot; lag panel `k ∈ {6, 9, 12}` joined as regressors with X-back-extension into 2014-01 → 2014-12 to preserve Y_eff = 2015-01 → 2026-03 = N=134 post-lag-12 per spec §4 v1.0.1 X-back-extension justification). Sign expectation pre-pinned positive (Baumol → US-Colombia tech-labor wage arbitrage → US tech-firm offshoring of Section J ICT services → Section J young-worker employment share rises at lag).

**Tech stack (specialist-side, advisory):** Python pandas + statsmodels for OLS; arch package for GARCH-X if escalation triggers; DuckDB for panel storage per existing `contracts/scripts/` convention; Jupyter notebook with trio-checkpoint discipline (`feedback_notebook_trio_checkpoint`) and 4-part citation block (`feedback_notebook_citation_block`). **Phase 2.5 NEW**: Economist Analyst Claude Code skill (per `feedback_economist_analyst_post_analytics_role` memory; `rysweet/amplihack` repo; install pending user adjudication per global MCP rule) interprets notebook trio outputs through 6-school framework (Classical / Keynesian / Austrian / Behavioral / Monetarist / Neoclassical Synthesis).

**Stage discipline (per CLAUDE.md framework Ideal-scenario clause + spec §9.11):** This plan executes Stage 1 (empirical risk validation) only. Stage 2 (M-sketch on Panoptic, including iron-condor strike geometry calibration against compositional-accounting decomposition per spec §9.16) and Stage 3 (deployment, Superfluid + X402 + ERC-4337 vault per agent-3 synthesis memo) are downstream and out of scope. Exit gate: positive-β confirmation at conventional significance, OR convex-payoff escalation evidence per pre-pinned spec §3.3-§3.4 contingency.

**Anti-fishing invariants (immutable through this plan, inheriting spec §9):**

- Spec sha256 `456ba39e188d00bb17471359a5803d6aa8a40de3b3788f17294bab828a968204` pinned in Phase 0; governs every downstream artifact
- Methodology-break disposition pre-committed in spec §6 to {empalme factor / 2021 regime dummy R1} — restrict-to-≥2022 (which fails N_MIN=75 at N=39) is anti-fishing-banned
- §3 thresholds pre-pinned (one-sided α=0.05 PASS / α=0.10 ESCALATE-PASS / Clause-B numerics 0.5 / 1.0 / 3.0); switching to two-sided post-data anti-fishing-banned
- Escalation suite (§5.5 quantile / GARCH-X / EVT POT) pre-authorized BEFORE estimation; ESCALATE-PASS threshold pre-pinned with concrete numerics
- Compositional-accounting verdict-interpretation discipline per spec §9.16: Phase-3 result memo MUST disclose Section J ⊂ Pair D G-T ambiguity; Stage-2 dispatch (if invoked) MUST address decomposition explicitly
- §6 fallback (i) trigger evaluation IS itself a §9.5 HALT-disposition (per spec v1.0.1 MQS FLAG-3 closure); never auto-fallback
- 3-way review of result memo regardless of verdict (PASS / FAIL / ESCALATE / SUBSTRATE_TOO_NOISY)
- HALT-disposition protocol per `feedback_pathological_halt_anti_fishing_checkpoint` invoked at every HALT point: typed exception → disposition memo (≥3 pivot options) → user surface → user picks → CORRECTIONS block → 3-way review of CORRECTIONS revision; auto-pivot anti-fishing-banned
- Section-J-specific logit-derivative amplification acknowledgment per spec §5.1 v1.0.1: 2-6× larger logit-Y level error vs Pair D's broad-services range; Phase-3 result memo MUST acknowledge if R1 / R3 are inconsistent with primary

**Scope (per SPM D1 v1.0 closure):** ~2.5-3 calendar weeks at single-stream pace with parallel Phase-1 dispatch (**11-13 specialist-days unconditional** inheriting Pair D's 9.5-11 floor + Phase 2.5 EA integration adder + Phase 3 §10 narrative adder). **+2-3 days** if Section-J cell-pathology fires §6 fallback (i) §9.5 HALT (NEW path not present in Pair D — Y feasibility memo §1.1 cites 700-1,200 14-28 employed in Section J vs Pair D's 7,000-9,000 broad-services aggregate). **+1.5 days** if §3.3 ESCALATE-trigger fires. **Lower bound ~2 weeks** IF and only IF zero HALT cycles fire AND escalation does not trigger AND Section-J cell-pathology does not trigger AND Phase 2.5 EA install gate resolves to `skill-not-installed-orchestrator-applies-framework-manually` (saves ~1 day vs MCP discovery / install / verify). Pair D Phase-1 schema-stability HALTs (CORRECTIONS-α / α') are **structurally pre-empted** by inheriting Pair D Option-α' window 2015-01 → 2026-03 (DANE-canonical Rev.4 throughout via `RAMA4D_R4`); saves ~1 day vs Pair D actual realized timeline.

**Source artifacts (read before executing):**

- `contracts/docs/superpowers/specs/2026-05-04-dev-ai-stage-1-simple-beta-design.md` — spec v1.0.1, sha pinned in frontmatter
- `contracts/.scratch/2026-05-04-dev-ai-y-feasibility.md` — Y feasibility memo (Y_p / Y_s1 / Y_s2 / Y_s3 source)
- `contracts/.scratch/2026-05-04-economist-analyst-integration-synthesis.md` — Phase 2.5 EA skill 4-touchpoint framework basis (synthesis memo; load-bearing for Task 2.5.1 manual-framework-application path per D4 + D15 v1.0 closure)
- `contracts/.scratch/path-b-stage-2/phase-1/corrections_eta_decomposition.md` — decomposition discipline
- `contracts/.scratch/path-b-stage-2/phase-1/corrections_theta_substrate_scope_clarification.md` — substrate-panel scope (a_l/a_s clarity)
- `contracts/.scratch/path-b-stage-2/phase-1/corrections_iota_a_s_framing.md` — per-user a_s framing
- `contracts/.scratch/2026-04-27-colombian-bpo-non-industrialization-hedge-research.md` — BPO literature pass (canonical citations apply since Section J is sub-aggregate)
- `contracts/notebooks/abrigo_y3_x_d/` — Pair D Option-β notebook trio scaffolding precedent
- `contracts/notebooks/fx_vol_cpi_surprise/Colombia/` — closed FX-vol-CPI pipeline (COP/USD reuse pattern + Banrep series provenance)
- `/home/jmsbpp/.claude/projects/-home-jmsbpp-apps-ThetaSwap-thetaSwap-core-dev--git-modules-lib-angstrom/memory/project_pair_d_phase2_pass.md` — Pair D PASS verdict (β=+0.137, p=1.46e-08, sha964c62cca…ef659) for §10 compositional-accounting disclosure verbatim citation per spec §9.16 (D15 v1.0 closure)
- `/home/jmsbpp/.claude/projects/-home-jmsbpp-apps-ThetaSwap-thetaSwap-core-dev--git-modules-lib-angstrom/memory/feedback_economist_analyst_post_analytics_role.md` — Phase 2.5 EA skill integration entry-point (D15 v1.0 closure)
- `/home/jmsbpp/apps/ThetaSwap/thetaSwap-core-dev/.worktree/ranFromAngstrom/CLAUDE.md` — framework section + Active iteration block + Pair D PASS verdict memory

---

## File Structure

**Created by this plan:**

```
contracts/.scratch/dev-ai-stage-1/
  data/
    geih_young_workers_section_j_share.parquet     Monthly Y_p (Section J)
    geih_young_workers_section_m_share.parquet     Monthly Y_s2 (Section M sensitivity)
    cop_usd_panel.parquet                          Monthly X with lag panel (incl 2014 back-extension)
    panel_combined.parquet                         Aligned (timestamp, Y_p, Y_s2, X_lags)
    DATA_PROVENANCE.md                             Sources + transformations
  notebooks/
    env.py                                         Path + dependencies + citation template
    references.bib                                 Lit anchors + spec sha256 + plan sha256
    01_data_eda.ipynb                              EDA per trio-checkpoint
    02_estimation.ipynb                            Primary OLS + verdict per trio-checkpoint
    03_tests_and_sensitivity.ipynb                 R1-R4 robustness + sensitivity per trio-checkpoint
  output/
    PRIMARY_RESULTS.md                             β̂, t-stat, p-value, sign per spec §3 decision tree
    ROBUSTNESS_RESULTS.md                          R1-R4 rows + AGREE/MIXED/DISAGREE classification
    ESCALATION_RESULTS.md                          Conditional on §3.3 ESCALATE-trigger
    MEMO.md                                        Stakeholder memo with verdict + Section-J compositional-accounting disclosure (§9.16) + Section-J logit-derivative amplification acknowledgment + Stage-2 dispatch readiness assessment
    gate_verdict.json                              Machine-readable verdict
    DISPOSITION.md                                 User decision artifact (Phase 4)
  output/multi_school_interpretation.md            NEW per Phase 2.5 (per SPM D23 v1.0 closure: standardized to output/ subfolder): Economist Analyst skill output applying 6-school framework + compositional-accounting + amplification + Stage-2 design rationale to notebook trio findings
```

**Modified by this plan:**

```
/home/jmsbpp/apps/ThetaSwap/thetaSwap-core-dev/.worktree/ranFromAngstrom/CLAUDE.md
  Abrigo Operating Framework "Active iteration" block updated post-Phase 3 with dev-AI-cost
  iteration verdict + disposition pointer + Section-J compositional-accounting disclosure.
```

---

## Phase 0: Spec verification + plan commit

Goal: confirm spec v1.0.1 is canonical at the pinned sha256, commit this plan under 2-wave verify, and dispatch Phase 1.

### Task 0.1: Verify spec sha256 + commit plan

**Owner:** Foreground Orchestrator (oversight); Reality Checker + Senior Project Manager (parallel 2-wave verifiers per `feedback_two_wave_doc_verification` Path-A: plan → RC + SPM)

**Files:**
- Verify: `contracts/docs/superpowers/specs/2026-05-04-dev-ai-stage-1-simple-beta-design.md`
- Commit: `contracts/docs/superpowers/plans/2026-05-04-dev-ai-stage-1-simple-beta-implementation.md` (THIS plan)

- [ ] **Step 1: Verify spec sha256 (non-destructive temp-copy protocol per SPM D10 + RC FLAG-1 v1.0 closure).** Run: `cp contracts/docs/superpowers/specs/2026-05-04-dev-ai-stage-1-simple-beta-design.md /tmp/spec_for_hash_verify.md && sed -i 's|decision_hash: 456ba39e188d00bb17471359a5803d6aa8a40de3b3788f17294bab828a968204|decision_hash: <to-be-pinned-after-recompute>|' /tmp/spec_for_hash_verify.md && sha256sum /tmp/spec_for_hash_verify.md`. Expected: hash equals `456ba39e188d00bb17471359a5803d6aa8a40de3b3788f17294bab828a968204` (the v1.0.1 sentinel-intact pin per spec frontmatter line 4 protocol). **DO NOT modify the spec file in-place.** If mismatch: HALT per spec §9.5 — spec has drifted from the pin; do not proceed to plan execution; surface to user via §9.5 HALT-disposition (typed exception `DevAIStage1SpecSha256DriftPathological`).

- [ ] **Step 2: Dispatch RC + SPM in parallel as Wave-1 + Wave-2 plan verifiers.** Per `feedback_two_wave_doc_verification` Path-A pattern (spec → RC + Model QA; plan → RC + SPM). RC reviews evidence-grounding + anti-fishing posture + load-bearing CORRECTIONS-η/θ/ι/§9.16 inheritance; SPM reviews task-decomposition realism + timeline + specialist-ownership clarity + per-task deliverable specificity. Both verdicts written to `/tmp/wave1_rc_dev_ai_plan.md` and `/tmp/wave2_spm_dev_ai_plan.md`. Expected: ACCEPT_WITH_FLAGS at minimum; full PASS preferred.

- [ ] **Step 3: Integrate verifier findings inline (if any).** If both verifiers ACCEPT, proceed to Step 4. If FLAGs/NITs surface, integrate into plan v1.1 with chunked discipline. If BLOCKs surface, HALT-disposition memo + user adjudication per `feedback_pathological_halt_anti_fishing_checkpoint`.

- [ ] **Step 4: Commit plan + push (HEREDOC pattern with Doc-Verify trailer per SPM D16 + D24 v1.0 closure).** Run: `git add contracts/docs/superpowers/plans/2026-05-04-dev-ai-stage-1-simple-beta-implementation.md` then commit via HEREDOC: `git commit -m "$(cat <<'EOF'` ... commit-message-with-Doc-Verify-trailer-citing-RC-and-SPM-agent-IDs+verdicts ... `Co-Authored-By: Claude <noreply@anthropic.com>` ... `EOF)"` then `git push origin phase0-vb-mvp` (per `feedback_push_origin_not_upstream` NON-NEGOTIABLE: push to **origin** = JMSBPP fork, NEVER upstream = wvs-finance).

- [ ] **Step 5: Update CLAUDE.md "Active iteration" block.** Document plan commit sha + plan-Wave-1+2 verdicts in CLAUDE.md Active iteration; commit + push under `feedback_two_wave_doc_verification` (RC + WA in parallel for CLAUDE.md edit).

---

### Task 0.2: EA skill installation pre-flight (NEW per SPM D4 v1.0 closure)

**Owner:** Foreground Orchestrator

**Files:**
- Create: `contracts/.scratch/2026-05-XX-economist-analyst-skill-install-decision.md` (user adjudication memo)
- Pin: user pick in plan v1.1+ frontmatter

- [ ] **Step 1: Verify EA repo exists.** Run `gh repo view rysweet/amplihack` to confirm canonical source; verify the synthesis memo `contracts/.scratch/2026-05-04-economist-analyst-integration-synthesis.md` exists at the cited path; if absent, BLOCK Phase 2.5 dispatch per D4(a). Document existence verification in install-decision memo.

- [ ] **Step 2: Enumerate install paths.** Per global CLAUDE.md MCP rule + 2026-05-04 user adjudication trail (Option F: research capabilities only): three install paths — (i) docker MCP install (PRIORITIZED per global rule but currently unavailable in this environment); (ii) github clone + manual install via `npx skillfish add rysweet/amplihack economist-analyst` or `uvx --from git+https://github.com/rysweet/amplihack amplihack <client>`; (iii) skill-not-installed orchestrator-applies-framework-manually using synthesis memo §3 four-touchpoint framework verbatim.

- [ ] **Step 3: Surface user adjudication memo.** Write `2026-05-XX-economist-analyst-skill-install-decision.md` enumerating the 3 paths + recommendation (default to path (iii) per current Option F adjudication unless user invokes change); user picks one path; pick is sha-pinned in plan v1.1+ frontmatter (or v1.2 if pick lands post-v1.1 commit).

- [ ] **Step 4: Pin user pick.** Update plan frontmatter with `ea_install_path_pin:` field naming the chosen path. Phase 2.5 Tasks 2.5.1-2.5.4 dispatch under the pinned path: if (i) or (ii), invoke skill directly; if (iii), Foreground Orchestrator applies framework manually citing synthesis memo §3.

---

## Phase 1: GEIH + COP/USD parallel data pull

Goal: emit `panel_combined.parquet` with monthly Y_p (Section J), Y_s2 (Section M), and X (COP/USD lag panel), aligned over 2015-01 → 2026-03 with N=134 post-lag-12 expected.

**Parallelization:** Tasks 1.1 + 1.2 run IN PARALLEL (single Foreground dispatch with two `Agent` calls in one message); Task 1.3 SEQUENTIALLY after both complete.

### Task 1.1: GEIH micro-data pull + Y_p (Section J) + Y_s2 (Section M) construction

**Owner:** Data Engineer

**Files:**
- Create: `contracts/.scratch/dev-ai-stage-1/data/geih_young_workers_section_j_share.parquet`
- Create: `contracts/.scratch/dev-ai-stage-1/data/geih_young_workers_section_m_share.parquet`
- Create: `contracts/.scratch/dev-ai-stage-1/data/DATA_PROVENANCE.md` (initial entries for GEIH)
- Reference (READ-ONLY): `contracts/docs/superpowers/specs/2026-05-04-dev-ai-stage-1-simple-beta-design.md` §5.1 Y construction pinned

- [ ] **Step 1: Schema-stability pre-flight.** Per `feedback_schema_pre_flight_must_verify_values` (NON-NEGOTIABLE) — verify per-file `RAMA4D_R4` values against DANE-published Section table for the entire 2015-01 → 2026-03 window. Per spec §5.1 v1.0.1 NIT-2 closure: Section J = 4-digit codes whose Division (first 2 digits) is in `{58, 59, 60, 61, 62, 63}`. Section M = 4-digit codes Division in `{69, 70, 71, 72, 73, 74, 75}` (per CIIU Rev.4 A.C. published Section table; verify primary-source reference). HALT-disposition if any month's `RAMA4D_R4` values are inconsistent with Rev.4: typed exception `DevAIStage1RAMA4DRev4ContradictionPathological` (analog of Pair D's `PairDEmpalmeSchemaContradictsHarmonizationPin`; per SPM D22 v1.0 closure: per-iteration audit-trail discipline requires fresh typed-exception name); this is structurally pre-empted by Option-α' window inheritance but verify anyway per `feedback_schema_pre_flight_must_verify_values`. **Per spec §9.14 (Free-tier methodology only, CORRECTIONS-δ inheritance), this Step MUST use only free-tier endpoints**; if rate-limit / paid-tier upgrade is required to unblock, HALT per §9.5 — do NOT silently pivot to paid-tier.

- [ ] **Step 2: Pull DANE GEIH micro-data 2015-01 → 2026-03.** Public CSV ZIP from DANE Microdata portal; no auth, no rate limit. ~135 monthly files. For each month: load full `Persona` table, filter to `Ocupado` employment status + age 14-28 inclusive (per Ley 1622 de 2013 "Estatuto de Ciudadanía Juvenil" + DANE's published youth-labor-market bulletin convention; per spec §1 line 63 + §5.1 line 161; per SPM D19 v1.0 closure: cite the statutory anchor + DANE bulletin for executor sub-agent reading task in isolation). Sensitivity at 18-28 / 18-24 / 15-24 is anti-fishing-banned per spec §9.2. Attach `RAMA4D_R4` 4-digit code + `FEX_C_2018` expansion factor, derive Section letter via DANE Section table. **Per spec §9.14 free-tier methodology only**: substituting a paid endpoint for a free-tier endpoint is anti-fishing-banned per §9.14.

- [ ] **Step 3: Construct Y_p (Section J).** For each month `t`, compute `Y_p_t (raw share) = sum over Section-J young-employed of FEX_C_2018 / sum over all young-employed of FEX_C_2018`. National aggregate (Cabecera + Resto summed). Apply DANE empalme factor for the 2015-01 → 2020-12 segment (per spec §6 primary disposition); 2021-01 → 2026-03 uses native FEX_C_2018 without empalme. Apply logit transform `Y_p_t (logit) = log(Y_p_t / (1 - Y_p_t))`.

- [ ] **Step 4: Construct Y_s2 (Section M sensitivity for §7 R2 robustness).** Same construction as Step 3 but with Section M instead of Section J. Logit transform. Both Y_p and Y_s2 emit per-month rows with `(year_month, raw_share, logit_share, FEX_normalized)`.

- [ ] **Step 5: Cell-size reporting (per RC NIT-2 v1.0 closure).** Emit per-month realized cell counts for Section J + Section M young-employed: how many distinct individuals (post-FEX-weighting) contribute to each section's numerator. Document in `DATA_PROVENANCE.md`. **Section-J cell pathology HALT trigger (per spec §6 v1.0.1 fallback (i) trigger restructuring + §9.5; SPM D7 v1.0 closure): the trigger is NOT a pre-pinned numeric threshold — it is `executing specialist documenting observed cell-size pathology` (observed monthly cell count visibly anomalous vs Pair D 7,000-9,000 / Y feasibility memo 700-1,200 baseline; OR observed HAC SE explosion in NB02 Task 2.3 Step 4; OR observed residual-variance instability).** The DE in this Step 5 emits per-month cell counts to `DATA_PROVENANCE.md` as a **diagnostic** and surfaces any visibly anomalous month to Foreground Orchestrator for §9.5 HALT-disposition adjudication; it does NOT auto-trigger HALT on any pre-pinned numeric. Anti-fishing rationale: a plan-pinned numeric threshold here would be the same footgun spec §6 FLAG-3 just removed (the v1.0 plan said "< 100" — that was the same anti-fishing footgun and is now retracted per SPM D7 closure). HALT-disposition memo enumerating pivot options (fallback (i) 3-month rolling / Y_s2 promotion to primary / Y_s1 EMS substitution / R1 promotion-to-primary) is invoked only on §9.5 HALT, not on threshold breach.

- [ ] **Step 6: Emit parquet artifacts.** Write `geih_young_workers_section_j_share.parquet` and `geih_young_workers_section_m_share.parquet` with sha256 hashes recorded. Schema: `(year_month, raw_share, logit_share, cell_count, FEX_total)`. Update `DATA_PROVENANCE.md` with sha256 + DANE source URLs + transformation log.

### Task 1.2: COP/USD panel from FX-vol-CPI pipeline (with X-back-extension into 2014)

**Owner:** Data Engineer

**Files:**
- Create: `contracts/.scratch/dev-ai-stage-1/data/cop_usd_panel.parquet`
- Update: `contracts/.scratch/dev-ai-stage-1/data/DATA_PROVENANCE.md` (append X entries)
- Reference (READ-ONLY): `contracts/notebooks/fx_vol_cpi_surprise/Colombia/` (Banrep series provenance precedent)

- [ ] **Step 1: Pull Banrep TRM end-of-month spot 2014-01 → 2026-03.** **Per spec §4 v1.0.1 X-back-extension** (NEW vs Pair D): X panel extends to 2014-01 to preserve Y_eff = 2015-01 → 2026-03 = 135 mo. Banrep TRM available continuously back to 1991-12; back-extension is mechanically trivial. Source URL: Banrep public TRM API (no auth, no rate limit observed per Y feasibility memo §1.1). Reuse Banrep series provenance from closed FX-vol-CPI pipeline (`contracts/notebooks/fx_vol_cpi_surprise/Colombia/`) verbatim. **Per spec §9.14 (Free-tier methodology only, CORRECTIONS-δ inheritance; SPM D11 + RC FLAG-4 v1.0 closure), this Step MUST use only free-tier endpoints; if rate-limit / paid-tier upgrade is required to unblock, HALT per §9.5 — do NOT silently pivot to paid-tier or to a paid mirror. Substituting a paid endpoint for a free-tier endpoint is anti-fishing-banned per §9.14.**

- [ ] **Step 2: Construct lag panel.** For each month `t ∈ [2015-01, 2026-03]`, compute `log(COP_USD_t)` and the lag panel `X_lag_6_t = log(COP_USD_{t-6})`, `X_lag_9_t = log(COP_USD_{t-9})`, `X_lag_12_t = log(COP_USD_{t-12})`. Lag-12 observations from 2014-01 → 2014-12 are consumed as regressors but the dependent-variable index starts at 2015-01.

- [ ] **Step 3: Emit parquet + provenance.** Write `cop_usd_panel.parquet` with schema `(year_month, log_cop_usd, log_cop_usd_lag6, log_cop_usd_lag9, log_cop_usd_lag12)`. Record sha256 + Banrep source URL + X-back-extension justification (per spec §4 v1.0.1). Update `DATA_PROVENANCE.md`.

### Task 1.3: Joint panel alignment + N verification

**Owner:** Data Engineer (sequential after Tasks 1.1 + 1.2)

**Files:**
- Create: `contracts/.scratch/dev-ai-stage-1/data/panel_combined.parquet`
- Update: `contracts/.scratch/dev-ai-stage-1/data/DATA_PROVENANCE.md` (append join entries)

- [ ] **Step 1: Inner-join Y_p + Y_s2 + X lag panel.** Schema: `(year_month, Y_p_logit, Y_p_raw, Y_s2_logit, Y_s2_raw, log_cop_usd, X_lag6, X_lag9, X_lag12, cell_count_section_j, cell_count_section_m)`. Index by `year_month` (monthly). Range: 2015-01 → 2026-03 (one-month tolerance for end-of-window publication-lag drop per spec §4).

- [ ] **Step 2: Verify N (per SPM D8 v1.0 closure: spec §3.6 sole gate is N < 75; orchestrator-adjudication-without-spec-basis is anti-fishing-banned).** Expected N = 134 post-lag-12 (per spec §4 v1.0.1 X-back-extension justification + Y feasibility memo §1.1). Per spec §3.6 the **SOLE numeric gate** is `N < N_MIN_OBS = 75 → HALT-N_MIN per §9.5`. If realized N < 75: typed exception `DevAIStage1NPathological`; HALT-disposition memo per §9.5. If realized N ≥ 75: PROCEED to Phase 2. **Diagnostic** (NOT a Phase-2 blocker): if realized N ∈ [75, 130] (i.e., > N_MIN floor but materially below expected 134), DE emits a per-month N-shortfall investigation note to `DATA_PROVENANCE.md` documenting *why* months were dropped (publication-lag / cell-pathology / merge artifact) — diagnostic record only. Anti-fishing: orchestrator-adjudication-without-spec-basis is banned per `feedback_pathological_halt_anti_fishing_checkpoint`; the spec §3.6 numeric is binding.

- [ ] **Step 3: Emit panel_combined.parquet + sha256-pin in DATA_PROVENANCE.** Final integrity check: per-row null-rate report; per-column type-stability check; per-month presence audit (no skipped months in 2015-01 → 2026-03 sequence).

- [ ] **Step 4: Phase 1 close commit + Gate.** Commit `panel_combined.parquet` + `DATA_PROVENANCE.md` final. **Gate B1 verification**: realized N + sha256 pins + cell-counts + sensitivity-Y N's all reported in `DATA_PROVENANCE.md`. Gate B1 PASS unblocks Phase 2 dispatch.

---

## Phase 2: Notebook trio under trio-checkpoint discipline

Goal: emit `01_data_eda.ipynb`, `02_estimation.ipynb`, `03_tests_and_sensitivity.ipynb` with trio-checkpoint HALT-and-review discipline (`feedback_notebook_trio_checkpoint`) and 4-part citation block (`feedback_notebook_citation_block`). Mirror `contracts/notebooks/abrigo_y3_x_d/` 3-NB scaffolding pattern (Pair D Option-β precedent).

**Trio-checkpoint discipline (NON-NEGOTIABLE per memory):** Analytics Reporter authoring a notebook HALTS after every (why-markdown, code-cell, interpretation-markdown) trio for human review. NO bulk authoring. Each trio is one HALT point. Pair D's Phase-2 CORRECTIONS-β remediation (script-form deviation discovered post-execution by Senior Developer 3-way review) is the cautionary precedent — this plan inherits the trio-checkpoint discipline verbatim from `contracts/notebooks/fx_vol_cpi_surprise/Colombia/` 3-NB pattern via Pair D Option-β.

### Task 2.1: Notebook scaffolding

**Owner:** Analytics Reporter (drafts skeleton); Foreground Orchestrator (verifies env.py + references.bib)

**Files:**
- Create: `contracts/.scratch/dev-ai-stage-1/notebooks/env.py`
- Create: `contracts/.scratch/dev-ai-stage-1/notebooks/references.bib`
- Create: `contracts/.scratch/dev-ai-stage-1/notebooks/01_data_eda.ipynb` (skeleton — title cell + table-of-contents only)
- Create: `contracts/.scratch/dev-ai-stage-1/notebooks/02_estimation.ipynb` (skeleton)
- Create: `contracts/.scratch/dev-ai-stage-1/notebooks/03_tests_and_sensitivity.ipynb` (skeleton)

- [ ] **Step 1: Author `env.py`.** Path injection (parents-fix per `865402c2c` precedent for `abrigo_y3_x_d/env.py`); statsmodels + pandas + arch + matplotlib imports; 4-part citation block template.

- [ ] **Step 2: Author `references.bib`.** Spec sha256 pin + plan sha256 pin (post-Task 0.1 commit) + lit anchors (Mendieta-Muñoz 2017 / Beerepoot-Hendriks 2013 / Errighi-Bodwell ILO 2017 / Baumol-Bowen 1966 / Rodrik 2016 / McMillan-Rodrik 2011) + Pair D PASS verdict citation (β=+0.137 p=1.46e-08).

- [ ] **Step 3: Skeleton notebooks.** Each notebook starts with: title cell + 4-part citation block + table-of-contents + section headers per spec §5 / §6 / §7 / §8. NO content cells yet — content authored under trio-checkpoint in subsequent tasks.

- [ ] **Step 4: Commit scaffolding.** `git add contracts/.scratch/dev-ai-stage-1/notebooks/` + commit message `nb(dev-ai iter): Phase-2 notebook trio scaffolding`. Doc-Verify trailer: `orchestrator-only-pre-Phase-3` per Pair D's deferred-trailer convention.

### Task 2.2: NB01 — Data EDA (trio-checkpoint dispatch)

**Owner:** Analytics Reporter (per-trio dispatch); Foreground Orchestrator (between-trio HALT review)

**Files:**
- Modify: `contracts/.scratch/dev-ai-stage-1/notebooks/01_data_eda.ipynb`
- Read: `contracts/.scratch/dev-ai-stage-1/data/panel_combined.parquet`

> **Trio anatomy reminder (NON-NEGOTIABLE per `feedback_notebook_trio_checkpoint`; SPM D5 + D12 v1.0 closure):** Each trio is `(why-markdown → code-cell → interpretation-markdown)`; HALT for orchestrator review after each trio. Do NOT collapse to code-only cells. Do NOT bulk-author multiple trios. Do NOT substitute script-form (`.py`) execution for notebook-form (`.ipynb`) execution — Pair D v2.4 CORRECTIONS-β precedent for this failure mode.

- [ ] **Step 0: Phase-2 script-form-deviation prophylactic (per SPM D5 v1.0 closure; Pair D v2.4 CORRECTIONS-β cautionary precedent).** Foreground Orchestrator confirms the dispatch instruction to Analytics Reporter explicitly mandates `.ipynb` file authoring, not `.py` script authoring; the AR sub-agent system prompt MUST include `feedback_notebook_trio_checkpoint` memory text verbatim and the dispatch MUST quote `each trio is one HALT point — NO bulk authoring; NO script-form substitution`. Foreground Orchestrator inspects the AR's first-trio output before the second trio dispatches; if the first-trio output is a `.py` file or a multi-trio batch, HALT per §9.5 and re-dispatch with the discipline-violation surfaced to user.

- [ ] **Step 1: Trio 1 — panel summary statistics.** Why-markdown: panel coverage + N=134 verification + per-column null-rate. Code-cell: load `panel_combined.parquet`, emit pandas describe(); plot Y_p_logit time series. Interpretation-markdown: confirm N matches §3.6 expectation; flag any month-gaps; HALT for orchestrator review.

- [ ] **Step 2: Trio 2 — Section J cell-size temporal stability.** Why-markdown: §6 v1.0.1 cell-pathology HALT trigger surface (count < 100 → §9.5 disposition). Code-cell: plot per-month cell counts + rolling mean; histogram. Interpretation-markdown: confirm no month falls below cell-pathology threshold; HALT for orchestrator review.

- [ ] **Step 3: Trio 3 — Y_p logit-derivative range visualization.** Why-markdown: §5.1 v1.0.1 Section-J-specific amplification — `d/dY[logit(Y)] = 1/[Y(1-Y)]` ranges 11-26 across [0.04, 0.10] (vs Pair D's 4-5 across [0.55, 0.75]). Code-cell: plot `1/[Y(1-Y)]` against realized Y; compute realized derivative ratio. Interpretation-markdown: confirm 2.34× ratio holds in realized data (not pre-data conjecture violated); cross-reference §6 empalme-residual-bias acknowledgment.

- [ ] **Step 4: Trio 4 — X (COP/USD) lag-panel autocorrelation.** Why-markdown: §5.3 multicollinearity — lag 6/9/12 of log(COP/USD) are AR(1) ≈ 0.95+, inflating individual `Var(β̂_k)` but composite SE is deflated by negative covariance. Code-cell: ACF plot of log(COP_USD) at lags 1-24; pairwise correlation matrix of X_lag_6 / X_lag_9 / X_lag_12. Interpretation-markdown: confirm AR(1) > 0.95; document expected composite-SE deflation.

- [ ] **Step 5: Commit NB01.** `git add contracts/.scratch/dev-ai-stage-1/notebooks/01_data_eda.ipynb` + commit. Doc-Verify trailer: `orchestrator-only-pre-Phase-3`.

### Task 2.3: NB02 — Primary OLS estimation + verdict (trio-checkpoint dispatch)

**Owner:** Analytics Reporter (per-trio dispatch); Foreground Orchestrator (between-trio HALT review)

**Files:**
- Modify: `contracts/.scratch/dev-ai-stage-1/notebooks/02_estimation.ipynb`
- Read: `contracts/.scratch/dev-ai-stage-1/data/panel_combined.parquet`
- Emit: `contracts/.scratch/dev-ai-stage-1/output/PRIMARY_RESULTS.md` + `gate_verdict.json`

> **Trio anatomy reminder (NON-NEGOTIABLE per `feedback_notebook_trio_checkpoint`; SPM D5 + D12 v1.0 closure):** Each trio is `(why-markdown → code-cell → interpretation-markdown)`; HALT after each trio. Do NOT collapse to code-only. Do NOT bulk-author. Do NOT substitute `.py` scripts — Pair D v2.4 CORRECTIONS-β precedent.

- [ ] **Step 0: Phase-2 script-form-deviation prophylactic (per SPM D5 v1.0 closure).** Foreground Orchestrator confirms dispatch mandates `.ipynb` authoring + per-trio HALT; inspects first-trio output before second-trio dispatch; HALT + re-dispatch on script-form substitution.

- [ ] **Step 1: Trio 1 — Methodology-break disposition (empalme primary; R1 dummy alternative).** Why-markdown: spec §6 primary disposition. Code-cell: apply DANE empalme factor to 2015-01 → 2020-12 segment of Y_p; native FEX_C_2018 for 2021-01 → 2026-03; emit empalme-applied Y_p panel. Interpretation-markdown: confirm empalme application; surface any anomalies. **If anomalies surface (per SPM D14 v1.0 closure): HALT per spec §9.5 + §6 fallback (i) trigger evaluation — typed exception → disposition memo enumerating fallback (i) 3-month-rolling / fallback (ii) quarterly UNVIABLE / Y_s2 promotion / R1 promotion-to-primary as ≥3 pivot options → user picks → CORRECTIONS block → 3-way review of CORRECTIONS revision. Auto-fallback to (i) is anti-fishing-banned per spec §6 FLAG-3 closure.**

- [ ] **Step 2: Trio 2 — Primary OLS estimation per spec §5.3.** Why-markdown: spec §5.3 OLS specification + §3 hypothesis. Code-cell: fit `Y_p_logit_t = α + β_6·X_lag_6 + β_9·X_lag_9 + β_12·X_lag_12 + ε_t`; compute composite β = β_6 + β_9 + β_12; compute composite SE via linear-restriction variance `c'Σc` for `c = (0, 1, 1, 1)'`; one-sided p-value. Interpretation-markdown: report composite β, t-stat, one-sided p; map to spec §3 verdict primitive (PASS / FAIL / ESCALATE Clause A or B).

- [ ] **Step 3: Trio 3 — Verdict mapping per spec §8.1.** Why-markdown: deterministic verdict tree §8.1. Code-cell: apply §8.1 mapping rules in order (1 N gate → 2 R-consistency [from Task 2.4] → 3 primary evaluation a-e); emit `PRIMARY_RESULTS.md` + `gate_verdict.json`. Interpretation-markdown: surface verdict; if ESCALATE-trigger fires, dispatch §5.5 escalation suite (conditional Task 2.4); if PASS, surface compositional-accounting disclosure per §9.16.

- [ ] **Step 4: Multicollinearity acknowledgment.** Per spec §5.3 v1.0 closure: result memo MUST flag "individual lags weren't significant!" reading as methodologically incorrect. Append interpretation-markdown trio explicitly stating composite-test framing + negative-covariance offset.

- [ ] **Step 5: Commit NB02.** `git add contracts/.scratch/dev-ai-stage-1/notebooks/02_estimation.ipynb output/PRIMARY_RESULTS.md output/gate_verdict.json`. Doc-Verify trailer: `orchestrator-only-pre-Phase-3`.

### Task 2.4: NB03 — Robustness R1-R4 + sensitivity + escalation (CONDITIONAL)

**Owner:** Analytics Reporter (per-trio dispatch); Foreground Orchestrator (between-trio HALT review)

**Files:**
- Modify: `contracts/.scratch/dev-ai-stage-1/notebooks/03_tests_and_sensitivity.ipynb`
- Read: `contracts/.scratch/dev-ai-stage-1/data/panel_combined.parquet`
- Emit: `contracts/.scratch/dev-ai-stage-1/output/ROBUSTNESS_RESULTS.md` + (conditionally) `ESCALATION_RESULTS.md`

> **Trio anatomy reminder (NON-NEGOTIABLE per `feedback_notebook_trio_checkpoint`; SPM D5 + D12 v1.0 closure):** Each trio is `(why-markdown → code-cell → interpretation-markdown)`; HALT after each trio. Do NOT collapse / bulk-author / substitute `.py` — Pair D v2.4 CORRECTIONS-β precedent.

- [ ] **Step 0: Phase-2 script-form-deviation prophylactic (per SPM D5 v1.0 closure).** Foreground Orchestrator confirms dispatch mandates `.ipynb` authoring + per-trio HALT; inspects first-trio output before second-trio dispatch; HALT + re-dispatch on script-form substitution.

- [ ] **Step 1: Trio 1 — R1 (2021 regime dummy).** Why-markdown: spec §7 R1 + §6 alternative methodology-break disposition. Code-cell: re-estimate primary OLS with `regime_2021 = 1{t ≥ 2021-01}` added as regressor (replaces empalme application of Step 1 in Task 2.3); emit β_composite_R1 + sign. Interpretation-markdown: AGREE/DISAGREE with primary sign.

- [ ] **Step 2: Trio 2 — R2 (Y narrow Section M).** Why-markdown: spec §7 R2 + §5.1 Y_s2 sensitivity. Code-cell: re-estimate primary OLS with Y_s2_logit (Section M young-worker share) instead of Y_p_logit (Section J); emit β_composite_R2 + sign. Interpretation-markdown: AGREE/DISAGREE; note that sign-flipped R2 indicates §1 mechanism is concentrated in ICT-narrow rather than knowledge-worker-broad.

- [ ] **Step 3: Trio 3 — R3 (raw OLS, no logit transform).** Why-markdown: spec §7 R3 + §5.1 logit-amplification sensitivity. Code-cell: re-estimate primary OLS with raw Y_p (not logit-transformed); emit β_composite_R3 + sign. Interpretation-markdown: AGREE/DISAGREE; note that sign-flipped R3 implicates logit-derivative amplification (§5.1 v1.0.1 acknowledgment fires).

- [ ] **Step 4: Trio 4 — R4 (HAC standard errors, Newey-West L=12).** Why-markdown: spec §7 R4. Code-cell: same primary point estimate but recompute SE via Newey-West with L=12; emit β_composite_R4 + HAC-SE-recomputed one-sided p-value. Interpretation-markdown: AGREE/DISAGREE; HAC autocorrelation diagnostic.

- [ ] **Step 5: Trio 5 — R-row consistency classification (§7.1) + SUBSTRATE_TOO_NOISY check.** Why-markdown: §7.1 AGREE/MIXED/DISAGREE rule. Code-cell: tally R1-R4 sign-match-vs-primary; if ≥3 of 4 sign-flipped → SUBSTRATE_TOO_NOISY per §3.5. Interpretation-markdown: emit consistency verdict; route to §8.1 step 2.

- [ ] **Step 6 (CONDITIONAL): Trio 6 — Escalation suite if §3.3 ESCALATE-trigger fires.** Per spec §5.5: D-i quantile τ=0.90 + D-ii GARCH(1,1)-X + D-iii EVT POT. Each emits coefficient + one-sided p ≤ 0.10 evaluation. ESCALATE-PASS if ANY ONE of D-i/ii/iii passes per §3.4 disjunction. Emit `ESCALATION_RESULTS.md`. **NOT a rescue claim** — pre-pinned per spec §9.6.

- [ ] **Step 7: Commit NB03 + outputs.** `git add contracts/.scratch/dev-ai-stage-1/notebooks/03_tests_and_sensitivity.ipynb output/ROBUSTNESS_RESULTS.md output/ESCALATION_RESULTS.md` (if exists). Doc-Verify trailer: `orchestrator-only-pre-Phase-3`.

---

## Phase 2.5: Economist Analyst skill multi-school interpretation (NEW)

Goal: apply the Economist Analyst Claude Code skill (`rysweet/amplihack`) to the notebook trio outputs to produce a multi-school economic interpretation per `feedback_economist_analyst_post_analytics_role` memory.

**Phase 2.5 decomposition per SPM D9 v1.0 closure:** Task 2.5.1 (was 5 steps in v1.0) decomposed into 4 cohesive sub-tasks (2.5.1 / 2.5.2 / 2.5.3 / 2.5.4) preserving per-task specialist-ownership pattern (`feedback_specialized_agents_per_task`). Phase 0 Task 0.2 EA install-gate elevation already handled the skill-install-gate (was Step 1 of v1.0 Task 2.5.1).

### Task 2.5.1: Six-school framework application to Stage-1 verdict

**Owner:** Foreground Orchestrator (under EA install path pinned in Phase 0 Task 0.2: invoke skill directly if installed; manual-framework-application from synthesis memo §3 if Option F path)

**Files:**
- Read (ALL): `contracts/.scratch/dev-ai-stage-1/output/PRIMARY_RESULTS.md` + `ROBUSTNESS_RESULTS.md` + (conditionally) `ESCALATION_RESULTS.md` + `gate_verdict.json`
- Read: `contracts/.scratch/dev-ai-stage-1/notebooks/02_estimation.ipynb` + `03_tests_and_sensitivity.ipynb`
- Read: `contracts/.scratch/2026-05-04-economist-analyst-integration-synthesis.md` §3 four-touchpoint framework (per D4 + D15 v1.0 closure)
- Create: `contracts/.scratch/dev-ai-stage-1/output/multi_school_interpretation.md` (per SPM D23 v1.0 closure: standardize path under `output/`)

- [ ] **Step 1: Apply six-school framework to Stage-1 verdict** per synthesis memo §3 Touchpoint 1 — emit per-school interpretation:
  - **Classical**: substitution effects in Colombian Section J hiring under FX-driven wage arbitrage; equilibrium-restoring labor flow into ICT services as USD/COP rises.
  - **Keynesian**: effective demand for Section J labor under FX shocks; Colombian aggregate demand for ICT responds to both domestic-currency and foreign-currency-denominated outsourcing demand.
  - **Austrian**: capital-structure distortion from cheap-credit-funded US AI tooling (FRED FEDFUNDS 2009-2022 era); structural distortion propagates to LATAM developer labor demand.
  - **Behavioral**: developer adoption-cycle non-rationality (FOMO-driven AI-tool subscription stacking; sunk-cost-fallacy in seat-license commitments).
  - **Monetarist**: USD money-supply expansion (post-2020 QE; post-2022 QT) drives USD/COP via interest-rate-differential and capital-flow channels.
  - **Neoclassical Synthesis**: combined mainstream framing reconciling above lenses.

- [ ] **Step 2: Emit Step-1 output as Section §1 of `multi_school_interpretation.md`.** This is the foundational lens-application; Tasks 2.5.2 and 2.5.3 append to this document.

### Task 2.5.2: Compositional-accounting decomposition (per spec §9.16 binding)

**Owner:** Foreground Orchestrator (under EA install path pinned in Phase 0 Task 0.2)

**Files:**
- Read: spec §9.16 v1.0.1 + `~/.claude/.../memory/project_pair_d_phase2_pass.md` (verbatim citation source for β_composite_pair_D_inherited)
- Append: `contracts/.scratch/dev-ai-stage-1/output/multi_school_interpretation.md` §2 Compositional-accounting decomposition

- [ ] **Step 1: Apply EA framework to Section J ⊂ Pair D G-T compositional risk.** Surface verdict-interpretation ambiguity between (i) independent dev-AI mechanism firing vs (ii) compositional re-discovery of Pair D's broad-services PASS aggregated up to Section J.

- [ ] **Step 2: Emit numeric comparison.** Read β_composite_pair_D_inherited = +0.13670985 (HAC SE 0.02465; t = +5.5456; p_one = 1.46e-08; spec sha256 `964c62cca…ef659`) **VERBATIM** from `~/.claude/.../memory/project_pair_d_phase2_pass.md` — DO NOT synthesize the Pair D numbers; cite the memory. Compare against β_composite_section_J from Task 2.3 Step 3 PRIMARY_RESULTS.md.

- [ ] **Step 3: Emit flagged-not-resolved status** on independent-mechanism-vs-re-discovery question. This is load-bearing input for Phase 3 MEMO §10 + Stage-2 dispatch readiness assessment per spec §9.16 binding.

- [ ] **Step 4: Surface R5 conditional-gate routing.** Per spec §9.16 v1.0.1: R5 robustness arm (primary spec on Sections G-T minus J) is PRE-AUTHORIZED for v1.1 spec revision under conditional gate. Surface in this section whether the realized data motivates R5 invocation (e.g., β_section_J ≈ β_pair_D suggests compositional; β_section_J >> β_pair_D suggests independent-mechanism amplification).

### Task 2.5.3: Section-J logit-derivative amplification interpretation (per spec §5.1 v1.0.1)

**Owner:** Foreground Orchestrator (under EA install path pinned in Phase 0 Task 0.2)

**Files:**
- Read: spec §5.1 v1.0.1 + Task 2.4 R1 + R3 robustness output
- Append: `contracts/.scratch/dev-ai-stage-1/output/multi_school_interpretation.md` §3 Logit-derivative amplification

- [ ] **Step 1: Apply EA framework to logit-derivative amplification.** Section J range [0.04, 0.10] yields derivative ratio 2.34× vs Pair D's [0.55, 0.75] ratio 1.32×; empalme residual amplifies 2-6× more in logit-Y at Section J range. Apply Classical / Austrian / Behavioral lenses to interpret whether observed R1 / R3 inconsistency vs primary signals empalme-residual-bias amplification.

- [ ] **Step 2: Cross-reference Task 2.4 R1 / R3 results.** If R1 (2021 dummy) sign-flips primary: empalme residual is the load-bearing source; logit amplification is observable. If R3 (raw OLS) sign-flips primary: logit transform itself is producing spurious signal; raw-share linearity is the cleaner story.

- [ ] **Step 3: Emit assessment** as §3 of `multi_school_interpretation.md`.

### Task 2.5.4: Final assembly + commit

**Owner:** Foreground Orchestrator

**Files:**
- Finalize: `contracts/.scratch/dev-ai-stage-1/output/multi_school_interpretation.md`

- [ ] **Step 1: Final assembly.** Concatenate §1 (six-school) + §2 (compositional) + §3 (amplification); add §4 Stage-2 design-rationale summary citing synthesis memo §3 Touchpoints 2 + 3 (Stage-2 instrument-design rationale + Stage-3 distributional implications).

- [ ] **Step 2: Commit `multi_school_interpretation.md` + Doc-Verify trailer.** Per Phase 0 Task 0.2 pinned EA install path: trailer `orchestrator-applied-EA-framework` if Option F; trailer `EA-skill-invoked` if installed. Push to origin.

---

## Phase 3: Memo + verdict + 3-way review

Goal: synthesize Phase 2 + Phase 2.5 outputs into a stakeholder result memo with 3-way implementation review (CR + RC + SD per `feedback_implementation_review_agents`); commit final + update CLAUDE.md.

### Task 3.1: Draft result memo

**Owner:** Analytics Reporter (drafts; per `feedback_specialized_agents_per_task` NON-NEGOTIABLE: Foreground Orchestrator never authors plan-task content; SPM D6 v1.0 closure restoring Pair D precedent). Foreground Orchestrator dispatches AR with full read of Phase 2 + 2.5 outputs; verifies §1-§10 section coverage; integrates 3-way review findings inline post-Task-3.2.

**Files:**
- Create: `contracts/.scratch/dev-ai-stage-1/output/MEMO.md`
- Read: all Phase 2 + Phase 2.5 outputs

- [ ] **Step 1: Draft MEMO sections §1-§10.** Mirror Pair D MEMO structure verbatim, plus dev-AI-iteration-specific sections:
  - §1 Executive summary (verdict + composite β + p + sign)
  - §2 Hypothesis recap (spec §2 verbatim)
  - §3 Sample (N realized, window, lag panel)
  - §4 Primary OLS results (β_composite, SE, t-stat, one-sided p, individual lag β's with multicollinearity acknowledgment)
  - §5 Robustness pack (R1-R4 + AGREE/MIXED/DISAGREE)
  - §6 Escalation suite (CONDITIONAL — only if §3.3 fired)
  - §7 Verdict per spec §8.1 deterministic mapping
  - §8 Section-J cell-size + logit-derivative amplification disclosure (per §5.1 v1.0.1 + §6 v1.0.1)
  - §9 Multi-school interpretation summary (cite Phase 2.5 `multi_school_interpretation.md`)
  - **§10 Compositional-accounting disclosure (per spec §9.16 v1.0.1, NEW vs Pair D; SPM D13 v1.0 closure)** — required for any Stage-1 PASS / ESCALATE-PASS verdict (and recommended for FAIL too as a diagnostic). Three sub-elements per spec §9.16: (a) explicit acknowledgment of compositional-accounting ambiguity (Section J ⊂ Pair D Section G–T); (b) numeric comparison of β_composite_section_J vs β_composite_pair_D_inherited = +0.13670985 (HAC SE 0.02465; t = +5.5456; p_one = 1.46e-08; spec sha256 `964c62cca…ef659`) read **VERBATIM** from `~/.claude/.../memory/project_pair_d_phase2_pass.md` — DO NOT synthesize the Pair D numbers; cite the memory; (c) flagged-not-resolved status on independent-mechanism-vs-re-discovery question; (d) R5 robustness arm conditional-gate routing language per spec §9.16 (R5 = primary spec on Sections G–T minus J, PRE-AUTHORIZED for v1.1 spec revision under conditional gate). Stage-2 dispatch (if invoked downstream of Stage-1 PASS / ESCALATE-PASS) MUST address the decomposition explicitly per spec §9.16 binding.

- [ ] **Step 2: Pin `gate_verdict.json` sha256 in MEMO frontmatter.** Reproducibility anchor.

### Task 3.2: Three-way implementation review

**Owner:** Foreground Orchestrator dispatches Code Reviewer + Reality Checker + Senior Developer in parallel per `feedback_implementation_review_agents`

**Files:**
- Reviewers READ: all Phase 2 / 2.5 / 3 outputs + spec v1.0.1 + plan v1.0
- Reviewers EMIT: `/tmp/dev_ai_stage_1_cr_review.md` / `/tmp/dev_ai_stage_1_rc_review.md` / `/tmp/dev_ai_stage_1_sd_review.md`

- [ ] **Step 1: Dispatch CR + RC + SD in parallel (per SPM D17 v1.0 closure: explicit §9.16 + Section-J Division-list + script-form-deviation reviewer charges).** CR reviews notebook code-cell correctness + statsmodels usage + numerical reproducibility (**including verification that Section-J Division indicator implementation matches spec §5.1 Division-list `{58, 59, 60, 61, 62, 63}`**); RC reviews evidence-grounding + anti-fishing posture preservation + spec compliance (**including specific verification that MEMO §10 satisfies spec §9.16 (a)+(b)+(c)+(d) sub-elements** — compositional-accounting acknowledgment, β_composite_section_J vs β_composite_pair_D_inherited verbatim citation from `project_pair_d_phase2_pass` memory, flagged-not-resolved status, R5 conditional-gate routing); SD reviews implementation discipline (trio-checkpoint actually applied? Doc-Verify trailers consistent? sha256 governance honored? **Phase-2 script-form-deviation precedent per Pair D v2.4 CORRECTIONS-β NOT recurring?**). Each writes verdict to `/tmp/dev_ai_stage_1_{cr,rc,sd}_review.md`.

- [ ] **Step 2: Integrate findings.** If all 3 reviewers ACCEPT or ACCEPT_WITH_FLAGS: surface verdict to user; proceed to Phase 4. If any BLOCK: HALT-disposition memo; user adjudication; CORRECTIONS-block; re-execute affected Phase 2 / 2.5 / 3 task.

- [ ] **Step 3: Phase-2/3 deviation detection (per Pair D Phase-2 CORRECTIONS-β precedent).** SD specifically checks: were notebooks actually authored under trio-checkpoint discipline (HALT after every why-markdown / code-cell / interpretation-markdown trio), or was script-form execution silently substituted? Pair D's CORRECTIONS-β remediation (re-execute Phase-2 as notebooks) is the cautionary precedent. If deviation discovered: invoke CORRECTIONS-block remediation analog.

### Task 3.3: Final commit + CLAUDE.md update

**Owner:** Foreground Orchestrator

**Files:**
- Modify: `/home/jmsbpp/apps/ThetaSwap/thetaSwap-core-dev/.worktree/ranFromAngstrom/CLAUDE.md` "Active iteration" block
- Commit: all Phase 2 / 2.5 / 3 artifacts + `MEMO.md`

- [ ] **Step 1: Commit MEMO.md + multi_school_interpretation.md + 3-way review verdict integration.** Final Phase-3 commit with Doc-Verify trailer `3-way-review-complete` (CR + RC + SD).

- [ ] **Step 2: Update CLAUDE.md "Active iteration" block.** Mirror Pair D PASS verdict update format. Document: dev-AI-cost iteration verdict; β_composite + p + sign; R-consistency classification; spec sha256 + plan sha256 + MEMO sha256; compositional-accounting disclosure pointer; Stage-2 dispatch readiness.

- [ ] **Step 3: 2-wave verify on CLAUDE.md update.** RC + WA in parallel per `feedback_two_wave_doc_verification`; integrate; commit + push.

---

## Phase 4: Disposition

Goal: surface verdict to user; user picks disposition (Stage-2 dispatch / pivot to sensitivity arm / kill iteration); document.

### Task 4.1: User disposition

**Owner:** Foreground Orchestrator

**Files:**
- Create: `contracts/.scratch/dev-ai-stage-1/output/DISPOSITION.md`

- [ ] **Step 1: Surface verdict to user (per SPM D18 v1.0 closure: 8-element ordered structured summary).** Structured summary in this exact order: (i) verdict label per spec §8.1 mapping (PASS / FAIL / ESCALATE-PASS / ESCALATE-FAIL / SUBSTRATE_TOO_NOISY / HALT-N_MIN); (ii) β_composite point estimate + HAC SE + t-stat + one-sided p-value; (iii) R-consistency classification per spec §7.1 (AGREE / MIXED / DISAGREE); (iv) compositional-accounting §10 disclosure summary including flagged-not-resolved status per spec §9.16; (v) Section-J logit-derivative amplification status (R1/R3 consistency vs primary per spec §5.1 v1.0.1); (vi) multi-school interpretation summary (Phase 2.5 `multi_school_interpretation.md` 1-paragraph excerpt); (vii) Stage-2 dispatch readiness assessment (PASS / ESCALATE-PASS → ready; otherwise → not ready); (viii) explicit option list for Step 2: (A) Stage-2 dispatch / (B) sensitivity-arm pivot / (C) kill-iteration. User picks one of A/B/C in Step 2.

- [ ] **Step 2: User picks disposition path.** Options:
  - **(A) Stage-2 dispatch** — verdict is PASS / ESCALATE-PASS; iteration is unblocked for Stage-2 M-sketch (iron-condor strike geometry on Panoptic). Per spec §9.16: Stage-2 dispatch brief MUST address Section J ⊂ Pair D G-T decomposition explicitly. Triggers R5 robustness arm conditional gate evaluation.
  - **(B) Sensitivity-arm pivot (per SPM D3 + RC FLAG-2 v1.0 closure: ESCALATE-FAIL re-ranking handoff explicit)** — verdict is FAIL or SUBSTRATE_TOO_NOISY; pivot to Y_s1 (DANE EMS Section J income index) or Y_s2 (already activated as R2; check sign-flip status) or Y_s3 (UNCTAD EBOPS-9 panel — note Y_s3 deferred FLAG-4 fires per spec §9.16 conditional gate). New Stage-1 spec authoring required (CORRECTIONS-block + 2-wave verify); the spec authoring is **gated on a fresh Trend Researcher dispatch** (or equivalent: Economist Analyst skill if installed; specialist-attribution memo otherwise) **to attribute *which mechanism assumption broke*** — Baumol disease vs US-Colombia ICT-labor wage arbitrage vs offshoring contracting cycle vs Section-J narrowing — and that attribution memo is the input artifact for the next-iteration Phase 0 spec choice between Y_s1 / Y_s2 / Y_s3 / revisit-mechanism. Auto-pivot to a sensitivity arm without mechanism-failure attribution is anti-fishing-banned per spec §9.5.
  - **(C) Kill iteration** — verdict is FAIL with no viable sensitivity pivot; close out per Phase-A.0 EXIT pattern (project memory `project_phase_a0_exit_verdict`); document closure rationale; user surfaces alternative iteration candidates.

- [ ] **Step 3: Document disposition.** Write `DISPOSITION.md` with user pick + rationale + next-step pointer + sha256-pinned memo references.

- [ ] **Step 4: Final close commit + push.** Iteration is closed at this point; downstream artifacts (Stage-2 spec / CORRECTIONS-block / EXIT close) are separate plans.

- [ ] **Step 5: Closure-archival (per SPM D2 v1.0 closure; Pair D v2 RC D6 precedent).** If verdict = FAIL after ESCALATE-FAIL OR SUBSTRATE_TOO_NOISY AND user disposition = kill-iteration (Option C), archive `contracts/.scratch/dev-ai-stage-1/` to `contracts/.scratch/archive-2026-05-dev-ai-stage-1/` per the Phase-A.0 archive convention (`project_phase_a0_exit_verdict` memory); update CLAUDE.md `Active iteration` block to point to archive location for posterity. If sensitivity-arm pivot (Option B) chosen, archive only on the next Y_p-iteration commit (avoid premature archival). If Option A chosen, retain `dev-ai-stage-1/` in active state pending Stage-2 spec authoring.

---

## Self-review checklist (run by orchestrator before commit of THIS plan)

- [ ] Spec coverage: every spec §1-§9 + §10 self-review item maps to a plan task. Spec §3 falsification primitives → Task 2.3 Step 3. Spec §5 methodology → Tasks 1.1 / 1.2 / 1.3 / 2.3. Spec §6 empalme + R1 + cell-size HALT → Tasks 1.1 / 2.3 / 2.4. Spec §7 R1-R4 → Task 2.4. Spec §8.1 verdict tree → Task 2.3 Step 3. Spec §9 anti-fishing → Phase 4 Task 4.1 disposition + Phase 3 Task 3.3 CLAUDE.md update. Spec §9.16 compositional accounting → Task 2.5.1 Step 3 + Task 3.1 §10.

- [ ] Placeholder scan: NO "TBD" / "TODO" / "implement later" / "fill in details" / "Add appropriate error handling" / "similar to Task N" patterns. Every code-cell / data-emission step describes WHAT the specialist produces, not HOW (per `feedback_no_code_in_specs_or_plans` code-agnostic mandate).

- [ ] Type consistency: Y_p_logit / Y_s2_logit naming consistent across Tasks 1.1 / 1.3 / 2.3 / 2.4. β_composite naming consistent. Cell-count column naming consistent.

- [ ] Specialist ownership: every task names exactly one specialist owner; foreground orchestrator only verifies/integrates per `feedback_specialized_agents_per_task`.

- [ ] HALT discipline: every HALT trigger maps to spec §9.5 protocol (typed exception → disposition memo → user surface → user pick → CORRECTIONS-block → 3-way review). Section-J cell-pathology HALT (Task 1.1 Step 5) + N_MIN HALT (Task 1.3 Step 2) + SUBSTRATE_TOO_NOISY HALT (Task 2.4 Step 5) + 3-way review BLOCK HALT (Task 3.2 Step 2) all map.

- [ ] Trio-checkpoint discipline: Phase 2 NB02 + NB03 every code-cell preceded by why-markdown + followed by interpretation-markdown; HALT for orchestrator review between trios per `feedback_notebook_trio_checkpoint`. Pair D Phase-2 CORRECTIONS-β script-form-deviation precedent surfaced as cautionary in Task 3.2 Step 3.

- [ ] Anti-fishing posture: spec §9 invariants explicitly carried; no thresholds adjustable post-data; one-sided test fixed; ESCALATE pre-authorization not post-hoc rescue; compositional-accounting verdict-interpretation discipline (§9.16) load-bearing in Phase 3 result memo.

- [ ] Free-tier methodology only (per CORRECTIONS-δ): all data sources (DANE GEIH CSV ZIP, Banrep TRM API, UNCTADstat CSV) free-tier accessible; no paid services in Phase 1-3.

- [ ] Phase 2.5 Economist Analyst integration: per `feedback_economist_analyst_post_analytics_role` memory pin 2026-05-04; skill-installation-gate handled per global CLAUDE.md MCP rule (option F: research capabilities only if not installed).

---

## Execution handoff

**Plan complete and saved to `contracts/docs/superpowers/plans/2026-05-04-dev-ai-stage-1-simple-beta-implementation.md`. Two execution options:**

**1. Subagent-Driven (recommended)** — Foreground Orchestrator dispatches a fresh subagent per task per `feedback_specialized_agents_per_task`; reviews between tasks; fast iteration. Sub-skill: `superpowers:subagent-driven-development`.

**2. Inline Execution** — Execute tasks in this orchestrator session using `superpowers:executing-plans`; batch execution with checkpoints for review.

Either way: Phase 1 Tasks 1.1 + 1.2 are PARALLELIZED at dispatch (single message with two `Agent` calls); Task 1.3 SEQUENTIALLY after both complete. Phase 2 NB-trio is sequential (each notebook authored under trio-checkpoint HALT discipline). Phase 2.5 is sequential after Phase 2. Phase 3 + Phase 4 sequential.

**Pre-flight before Phase 1 dispatch:**
- Plan committed (Task 0.1 Step 4)
- Plan 2-wave verify ACCEPT (Task 0.1 Step 2)
- Spec sha256 verified MATCH (Task 0.1 Step 1)
- CLAUDE.md "Active iteration" block updated (Task 0.1 Step 5)

End of plan.
