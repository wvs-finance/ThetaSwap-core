

## GOALS
We want to leverage an integration between Arnstrong and Panoptic:

- Arnstron allow us to dfine pools where MEV = 0
- Panoptic is a settlement layer which allows to realize any continous payoff as a custom combination of option legs





Vault V( A = 0) (lpCummulative) --> asset0
Vault V_{B =1 }(lpRewardTotal) --> asset1


V_{B =1}{
      deposit d --[ s(d, S (f(lpCummulative))) | lt(V(s(d)),V(d)) ]


      s(d, S(f (lpRewardCumm))) -> UNIT_ACCOUNT -> asset1
               |
                --->   d / S (f (lpRewardCum)) with some parameters

      S (f (lpRewarrdCum)) -> d S / d f(lpRewardCum) --> f ?

      Considerations for f ?
          ( f ? ) ( 1 ) :: f is INDEPENDENT on V( A =0 ) 
          ( f ? ) ( 2 ) :: What does S represent economically not as total Supply but the fact that
          is tracking total exposure to lpReward in some form defined by f
          ( f ? )  ( 3 ) :: Contingent on (2) what are some invariant properties f MUST respect
          with respect to S
          ( f ? ) ( 4 ):: What are some properties f MUST preserve with respect to lpRewardCumm

       

}



SHORTS --(margin) --> 



testPoolKey = _createPool( di, phi, L_0, \phi*);

