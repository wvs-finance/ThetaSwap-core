# Senior PM — Plan Rev-5.2 Review (Task 11.N.1 insert)

**Target:** `contracts/docs/superpowers/plans/2026-04-20-remittance-surprise-implementation.md` @ `b745e4e22`
**Diff:** `git diff 8c984ab56..b745e4e22` (82 new lines)
**Lens:** execution / dependency / scope / timeline / rollback

## 1. Verdict

**ACCEPT-WITH-FIXES.** Closes a load-bearing data gap via a sensible free on-chain-native path with fallback. But under-specifies 11.O's blocked/unblocked state, misses a parallelism opportunity, omits resumability for a 30–60 min pull, and packs a 4-script + schema-fork + new-dep scope into one task.

## 2. Scope/Execution findings

- **PM-P1 (DAG ambiguity — MAJOR).** 11.N's Gate (unchanged) says its surrogate X_d is "consumed by Task 11.O." 11.N.1's Gate says 11.O "can then consume both variants." Plan does NOT state whether 11.O is (a) BLOCKED on 11.N.1, (b) UNBLOCKED via surrogate, or (c) parallel. Determines critical path.
- **PM-P2 (Parallelism not declared — MAJOR).** 11.O is spec authoring — needs X_d's *definition*, not 110k rows. Real parallelism with 11.N.1 backfill; not stated.
- **PM-P3 (Rollback semantics absent — MAJOR).** "HALT + escalate `X_D_STILL_INSUFFICIENT`" does not say: (i) partial CSV revert; (ii) 10-row sample preservation; (iii) schema rollback. Step 1 itself forks ("new table vs `is_sample` flag — pick whichever is cleaner") — a scope-decision punt the 3-way review cannot pre-approve.
- **PM-P4 (Scope-creep — decomp recommended).** Touches `econ_pipeline.py` (2 fns + orchestrator), CSV replace, provenance README, `econ_schema.py` (new table OR flag), `econ_query_api.py` (loader semantics), test suite (5 new assertions), filter re-run, `onchain_xd_weekly` rows, + potential `web3.py` dep. Too large for "30–60 min."
- **PM-P5 (TDD rhythm — FLAG).** Step 1(e) couples Red step to live RPC. Permitted by `feedback_real_data_over_mocks.md` but RPC flake during test-drafting is indistinguishable from the legitimate Red failure. Flag explicitly.
- **PM-P6 (Timeline — FLAG).** ~1,900 × 10k-block windows × ~1–2 s/call ≈ 30–60 min best case, 2–3× under rate limits. Material vs EXIT clock; not flagged.
- **PM-P7 (Resumability absent — MAJOR).** No checkpointing. 60-min pull interrupted at block 12M restarts from 0. Fix: persist `(last_completed_block, row_count)` sidecar JSON.
- **PM-P8 (Dep governance — FLAG).** "`web3.py` OR `requests`" is ambiguous. Commit to `requests` (no new dep) or flag for user approval.
- **PM-P9 (Escalation — NIT).** Clarify "user" = orchestrator; subagent MUST return without committing partial progress.
- **PM-P10 (Task-count — PASS).** 56 + 1 = 57. No `11.N.1` ID collision in Phase 2b+.
- **PM-P11 (Gate tolerance — FLAG).** 110,069 ± 1% tolerance; others binary. State: all 4 criteria hold jointly; partial = FAIL.

## 3. Updated DAG (recommended)

```
11.L ──┐
11.M ──→ 11.M.5 ──→ 11.M.6 ──→ 11.N ──→ 11.O ──→ 11.P ──→ Phase 2b (Task 12)
                                  │         ↑
                                  └──→ [11.N.1] ──→ 11.Q (panel assembly)
```

Parallelism: `11.N.1 ∥ 11.O`. **11.O is already unblocked by 11.N's surrogate**; 11.N.1 feeds 11.Q + (optionally) an 11.O spec revision if lit review flags supply-channel X_d as inadequate. Plan should state this explicitly.

## 4. Decomposition recommendation

Split into 4 sub-tasks:
- **11.N.1a** — RPC primary + provenance README + smoke test (~45 min; dominated by pull)
- **11.N.1b** — Alchemy fallback (contingent; no-op if primary succeeds)
- **11.N.1c** — DuckDB schema decision (new table vs `is_sample` flag) + test extension; resolve the Step-1 fork into a concrete committed choice
- **11.N.1d** — Re-run `compute_weekly_xd()` on full data + persist `proxy_kind="b2b_to_b2c_net_flow_usd"` rows

Benefit: resumability (11.N.1a checkpoints), atomic rollback per sub-task, honest time-boxing.

## 5. Risk register (top 3)

1. **Both RPC paths fail after 30 min invested → HALT with no retry path.** Mitigation: pre-flight check for `ALCHEMY_API_KEY` at Step 1 of 11.N.1a; if absent, resolve now (commit to primary-only + accept fallback-impossible, or pull key).
2. **11.O blocks on 11.N.1 unnecessarily, compressing EXIT timeline.** Mitigation: declare 11.O's unblocked status explicitly; dispatch 11.N.1 and 11.O in parallel.
3. **Step-1 schema fork resolved at implementation time → 3-way review hasn't seen the final design.** Mitigation: pick NOW in the plan. Recommended: `is_sample` boolean column — strictly additive, preserves single-table semantics, supports sample + full rows side-by-side during transition.
