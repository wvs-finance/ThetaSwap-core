# Rev 5.2.1 Plan Fix-Log

**Plan target:** `contracts/docs/superpowers/plans/2026-04-20-remittance-surprise-implementation.md`
**Rev 5.2 commit:** `b745e4e22`
**Three-way review trio:** Code Reviewer + Reality Checker + Senior PM
**Consolidation author:** orchestrator (Rev-3.1 precedent)

---

## Reviewer reports

- CR: `contracts/.scratch/2026-04-24-plan-rev5.2-review-code-reviewer.md` — ACCEPT-WITH-FIXES (2 BLOCKERs + 4 MAJORs + 2 MINORs + 2 PASS-verified)
- RC: `contracts/.scratch/2026-04-24-plan-rev5.2-review-reality-checker.md` — **NEEDS WORK** (2 BLOCKs + 3 FLAGs + 2 NITs)
- PM: `contracts/.scratch/2026-04-24-plan-rev5.2-review-senior-pm.md` — ACCEPT-WITH-FIXES (3-risk register)

## Empirical verifications RC landed (no action needed)

- Forno.celo.org + Ankr RPC both LIVE; `eth_getLogs` returns 55 Transfer logs over 9,320-block smoke window ✓
- COPM `0xc92e8fc2947e32f2b574cca9f2f12097a71d5606` has non-zero bytecode (EIP-1967 proxy) ✓
- Transfer topic `0xddf252ad…b3ef` matches `keccak256("Transfer(address,address,uint256)")` ✓
- 110,069-row target grounded at `contracts/data/copm_per_tx/README.md:12` ✓
- Rev-4 `decision_hash = 6a5f9d1b…a443c` preserved at `nb1_panel_fingerprint.json:23` ✓

## BLOCKERs applied in Rev 5.2.1

| Severity | Source | Finding | Action |
|---|---|---|---|
| BLOCKER | CR-B1 / RC-B1 | `onchain_xd_weekly` CHECK at `econ_schema.py:367-374` pins `proxy_kind='net_primary_issuance_usd'` as a singleton; single-column `week_start` PK prevents coexistence of supply + distribution series | New **Step 0** prepended to Task 11.N.1: schema migration (relaxed CHECK + composite PK) committed BEFORE any RPC code; atomically rollback-able |
| BLOCKER | CR-B2 | `onchain_copm_transfers` table design ambiguous (new-table vs `is_sample` flag) | Pre-committed to new-table option; `onchain_copm_transfers_sample` preserved as audit pointer |

## MAJORs applied in Rev 5.2.1

| Severity | Source | Finding | Action |
|---|---|---|---|
| MAJOR | CR-MAJOR / RC-B2 | `web3.py` not in `contracts/.venv/` | Dropped `web3.py` references; plan specifies `requests`-only JSON-RPC (venv has `requests`, `eth-hash`, `eth-typing`, `eth-utils` which cover hex-decode needs) |
| MAJOR | RC-new | `copm_xd_filter.py:71` hard-codes `proxy_kind` constant | Step 0 parametrizes `compute_weekly_xd()` to accept `proxy_kind` argument |
| MAJOR | PM-P1 | DAG unclear: does 11.O block on 11.N.1? | Explicit "DAG clarification" block added: 11.O CAN run in parallel on supply-channel X_d; 11.N.1 folds distribution-channel via amendment-rider in 11.O Step 4 |
| MAJOR | PM-P2 | 30-60 min RPC pull lacks resumability | Per-10k-block checkpoint specified: `contracts/.scratch/copm_transfers_backfill_progress.json` with `{last_completed_end_block, total_rows_so_far, data_source}`; restart resumes from `last + 1` |

## MINORs / hygiene applied

| Severity | Source | Finding | Action |
|---|---|---|---|
| MINOR | CR | `.env.example` + `ALCHEMY_API_KEY` handling | Added `contracts/.env.example` to Files section; HALT-on-missing in Step 3 Alchemy fallback |
| MINOR | CR | `web3` dep missing from venv | Resolved via `requests`-only pin (no new deps) |

## Deferred (future pass)

| Source | Finding | Deferral reason |
|---|---|---|
| PM NIT | Decompose 11.N.1 into 11.N.1a-d sub-tasks | The expanded Step structure (Step 0 atomic + Steps 1-6 in sequence) achieves the atomic-rollback property without splitting task ID; saves ID-churn throughout plan |
| PM NIT | Cross-source reconciliation if RPC returns N rows, Alchemy returns N±k | Implementation-time decision; document in provenance README if ever triggered |
| CR MINOR | Test-smoke-vs-real-RPC flakiness risk | Use pinned small range (e.g., 40000000-40010000) as smoke test — stable across real RPC availability |
| CR MINOR | 10k-block window may exceed some endpoints' range caps | Binary-search narrowing in `fetch_copm_transfers_rpc()` already in fallback-trigger criterion (3) |
| RC FLAG | 109,900 rows (99.85%) below threshold won't trigger fallback | 1% tolerance is intentional floor; document in commit message |

## Next actions

1. Commit Rev-5.2.1 plan patch.
2. Dispatch Data Engineer for Task 11.N.1 (Steps 0-6 sequenced; schema migration first).
3. In parallel: dispatch Task 11.O structural-econometrics skill invocation using supply-channel X_d (distribution-channel will amendment-rider in later).
