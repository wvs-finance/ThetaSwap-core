# Rev-5.3 Consolidated Plan Review — Reality Checker

**Date:** 2026-04-24
**Target:** `contracts/.scratch/2026-04-24-plan-rev5.3-draft.md` (TW fix-pass + brainstorm-fold)
**Design doc:** `contracts/docs/superpowers/specs/2026-04-24-carbon-basket-xd-design.md`
**Prior review:** `contracts/.scratch/2026-04-24-plan-rev5.3-review-reality-checker.md`

## 1. Verdict — **NEEDS WORK** (BLOCKER on threshold internal-consistency)

Prior BLOCKers all landed correctly. The brainstorm-fold introduces TWO new BLOCKers: (a) the pre-committed thresholds **N_min=80 and power_min=0.80 are mutually unsatisfiable** at MDES=0.20 SD with k=13 regressors (empirically: power@n=80 ≈ 0.42, far below 0.80 — need n≈200 to clear power=0.80); (b) Mento-side basket symbols in plan §4 are STALE — canonical Celo token list shows the basket has been rebranded to {COPM, USDm, EURm, BRLm, KESm, XOFm}, not {COPM, cUSD, cEUR, cREAL, cKES, XOFm}.

## 2. Did your prior BLOCKers land?

| Prior finding | Landed? | Evidence |
|---|---|---|
| RC-B1: file references → `copm_transfers_full.csv` | **Yes** | Plan §3 lines 17, 28, 92-97, 100-101, 116-118, 130 all reference `copm_transfers_full.csv`; legacy `copm_transfers.csv` explicitly preserved as 17-line stub note (line 93) |
| RC-B2: 503 rationale rewrite + RPC-as-primary | **Yes** | Plan §3 line 18, 51, 59, 73, 101 all retired "503 stickiness"; path-1 = forno+Ankr (1k window for Ankr) per RC empirical evidence |
| RC-truncated USDT/WETH HALT-VERIFY | **Yes** | Plan §4 lines 158, 160, 205 install Step 1.1 5-min halt gate with explicit canonical addresses + 3-option resolution |
| RC-P12 Dune-credit 613,603 / 0.028 credits cite | **Yes** | Plan §4 line 60, 142, 206 — "non-issue" budget framing |

All prior BLOCKers correctly landed.

## 3. Empirical-verification battery (RC-CF-1..N)

| ID | Claim | Evidence | Pass/Fail |
|---|---|---|---|
| RC-CF-1 | CarbonController `0x6619...` live + decoded on Celo | `searchTablesByContractAddress`: 52 results, top hit `carbon_defi_celo.carboncontroller_evt_tokenstraded` page-rank 0.566 | **PASS** |
| RC-CF-2 | BancorArbitrage `0x8c05ea...` live + decoded on Celo | `searchTablesByContractAddress`: 16 results, all `carbon_defi_celo.bancorarbitrage_*` | **PASS** |
| RC-CF-3 | User-supplied `0xcd6b5c54b347b56fa0fb37f0b1eda69a76e29afb` is CarbonController on Celo | `searchTablesByContractAddress`: **0 results**; this address is NOT in Dune Celo decoded tables | **FAIL** — address not Carbon-related; user's check-list contained a wrong address |
| RC-CF-4 | All 6 Mento + 4 global addresses canonical | `tokens.erc20` Dune lookup returns all 9: COPM=`COP Minteo`, USDm=`Mento Dollar` (rebrand of cUSD), EURm=`Mento Euro` (rebrand of cEUR), BRLm=`Mento Brazilian Real` (rebrand of cREAL — note BRL not REAL!), KESm=`Mento Kenyan Shilling`, XOFm=`Mento West African CFA franc`, USDT, USDC, WETH (all decimals correct: 6 for USDT/USDC, 18 for rest) | **PASS w/ rebrand finding** — see §6 |
| RC-CF-5 | Carbon Dune fields use `varbinary` for addresses, not `varchar` | Live execution `lower(varbinary)` errored: "Lower is unnecessary for varbinary…" | **NEW FINDING** — plan §4 DDL pre-commits addresses → `VARCHAR(42)`. Mismatch with Dune storage; ingestion must HEX-encode at SELECT time (`'0x' \|\| TO_HEX(sourceToken) AS source_token` style) |
| RC-CF-6 | Decision-hash `6a5f9d1b…a443c` byte-exact preserved | `nb1_panel_fingerprint.json:23` reads exactly that hash | **PASS** |
| RC-CF-7 | `scripts/tests/inequality/` directory exists per plan claim | `ls`: directory exists with `test_copm_xd_filter.py`; ready for `test_carbon_calibration.py` + `test_carbon_xd_filter.py` | **PASS** |

## 4. Threshold-empirical-feasibility analysis (BLOCKER)

| Threshold | Plan value | Empirical reachability | Verdict |
|---|---|---|---|
| `N_min` (basket-aggregate non-zero weeks) | 80 | RC live Dune probe: **80 weeks** non-zero Sep-2024 → 2026-04-24 (basket-aggregate user-only, both protocol bots filtered). Exactly meets threshold today; one more week of zero events would breach it. | **MARGINAL PASS** — threshold IS reachable but at the absolute edge; budget no headroom |
| `N_min` (COPM-only non-zero weeks) | 80 | RC live probe: only **44 weeks** non-zero. **DEAD BRANCH** — calibration deterministically picks basket-aggregate. The user's pre-flagged dead-branch risk materialized empirically. | **FAIL** — COPM-only branch is unreachable; threshold must be data-relative (e.g., "N_min = 80 OR top-decile-currency"-conditional) per user push-back option |
| `power_min` (statistical power for MDES=0.20 SD, k=13) | 0.80 | scipy `nct.cdf` two-sided t-test n=80, df=80-13=67, ncp=0.20·sqrt(80)=1.79: **power ≈ 0.42**. Cohen f²=0.04 partial-F approach: **power ≈ 0.37**. To achieve power=0.80 at MDES=0.20 SD: need **n ≈ 200**. | **BLOCKER FAIL** — N_min=80 and power_min=0.80 are mutually unsatisfiable at MDES=0.20 SD. Resolution options: (a) raise N_min to 200; (b) relax MDES to ~0.30 SD; (c) lower power_min to 0.42. None is currently pre-committed. |
| `pc1_loading_floor` | 0.40 | PCA convention: |loading| ≥ 0.40 is widely cited (Hair et al. 2014, *Multivariate Data Analysis* 7th ed., p. 115; Kline 2016, *PCA in Practice*) as the "practically significant" loading threshold for moderate-N samples. 0.40 sits in the standard 0.30–0.50 conventional range. | **PASS** — defensible per common PCA-reporting convention |

## 5. Schema-migration-chain safety verdict — **HAS GAP**

The chain is: 11.N.2b.1 (test against in-memory) → 11.N.2b.2 (canonical commit on smoke-probe pass) → 11.N.2c (additive INSERT for primary `proxy_kind`).

- **11.N.2b.1 → .2b.2 transition:** atomic per Step Atomicity Protocol — temporary DuckDB throughout 11.N.2b.1; canonical untouched until .2b.2 Step 5 commit-point. **AIRTIGHT.**
- **11.N.2b.2 → .2c transition:** plan line 308 says calibration is READ-only on canonical; line 261 says "Steps 1-4 operate against TEMPORARY DuckDB; canonical untouched"; only Step 4 INSERT (line 307) mutates. **AIRTIGHT.**
- **GAP**: if 11.N.2c Step 4 INSERT begins (atomic single-transaction), then 11.N.2c Step 5 raises `CalibrationStructurallyPathological` mid-transaction, the rollback semantics are not specified. Plan should add: "any uncaught exception during Step 4 INSERT must trigger explicit `connection.rollback()` before re-raising, so canonical DB stays at pre-Step-4 state." Currently the wording is "wrapped in single transaction" without an explicit rollback contract.
- **Variation gap (RC-CF-5):** the DDL pre-commits `addresses → VARCHAR(42)` but Dune source columns are `varbinary`. Ingestion path must convert via `'0x' \|\| LOWER(TO_HEX(addr))` at SELECT-time; if implementer treats this as a Python-side coercion (`.hex()` on bytes), the schema-migration test will pass but the ingestion will produce different byte sequences than the design memo's golden-fixture expectations. **Add explicit DDL-vs-source-dtype reconciliation step in 11.N.2b.1 Step 2.**

## 6. New findings introduced by the consolidated draft

- **Mento basket symbol rebrand (NEW):** plan §4 lines 51, 60, 158, 162, etc., consistently call the basket `{COPM, cUSD, cEUR, cREAL, cKES, XOFm}` (Mento legacy). Canonical 2026-04-24 Celo token list shows the rebrand: `{COPM, USDm, EURm, BRLm, KESm, XOFm}`. Plan must either (a) update symbols throughout to the canonical names; (b) add a footnote pinning the contract addresses (immutable) over the symbol labels (rebrandable). Note BRLm not BRL_REAL — the Brazilian Real Mento is `BRLm` per canonical list, so `cREAL` is doubly stale.
- **User-only filter is too narrow:** RC live probe shows `n_unique_traders` per week often 1-3 even after excluding the two protocol bots. Recent weeks (2026-04) show only 1-2 unique traders. The "user-initiated" filter likely captures a tiny number of repeated automation accounts — possibly other bots. Plan §4 should add a bot-detection followup test (e.g., `n_distinct_traders >= 5 per week` as a sanity check; if violated, escalate before persisting).
- **Calibration dead-branch confirmed empirically:** user's push-back hypothesis fired. COPM-only weekly-non-zero count = 44 vs. N_min = 80. The COPM-only branch will deterministically fail under the current pre-committed threshold. Recommend either (a) lowering COPM-only N_min to ≈ 30 (data-driven minimum supporting 30-obs t-test power-sanity); (b) reframing N_min as "N_min = max(30, top-decile-Mento-currency-non-zero-weeks)" so the rule scales with what the data supports; (c) explicitly accepting the dead branch and removing Case 1 + Case 3 from §4 of the design doc — Case 2 always fires.
- **Y-target deferral safety:** plan A1-A5 amendment-rider queue defers Y-target shift to Rev-5.4. **A9 says 11.O DOES BLOCK on 11.N.2c**. This means 11.O cannot dispatch with the deferred Y-target rider unresolved. The deferral is internally inconsistent: 11.O cannot Author-spec without a Y, but the Y is queued for Rev-5.4. Either (a) 11.O must wait until Rev-5.4 amendment-rider lands (i.e., add explicit BLOCK on Rev-5.4 author-session); (b) 11.O Step 1 inputs a placeholder Y (USD-COP carry, the legacy Rev-5 formula) under explicit caveat — the Carbon X_d will then later need re-spec'ing once Y shifts. Pick one and document.
- **Active-link / file-path consistency:** new files referenced (`carbon_calibration.py`, `carbon_xd_filter.py`, `2026-04-24-carbon-basket-calibration-result.md`, `2026-04-24-carbon-xd-pre-commitment-memo.md`, `2026-04-24-carbon-xd-pathological-disposition.md`) all follow project convention (`contracts/scripts/`, `contracts/scripts/tests/inequality/`, `contracts/.scratch/YYYY-MM-DD-*.md`). **PASS.**

## 7. Required fixes summary

1. **BLOCKER (threshold internal-inconsistency):** N_min=80 ⊕ power_min=0.80 ⊕ MDES=0.20 SD cannot all hold. Fix one of {raise N_min to 200; relax MDES to 0.30 SD; lower power_min to 0.42}. Pre-commit the resolution in design doc §3 + plan §4 11.N.2c Step 0.
2. **BLOCKER (COPM-only dead branch):** with N_min=80 and only 44 actual non-zero COPM weeks, the COPM-only branch is unreachable. Adopt user's data-relative threshold or explicitly delete Case 1 + Case 3 from §4 design doc.
3. **MAJOR (symbol rebrand):** propagate `{USDm, EURm, BRLm, KESm}` Mento rebrand throughout plan + design doc, OR pin to addresses with footnote.
4. **MAJOR (varbinary DDL gap):** add Step 2 reconciliation for `varbinary→VARCHAR(42)` conversion; specify SQL-side hex encoding.
5. **MINOR (rollback contract):** Step 4 INSERT in 11.N.2c needs explicit `connection.rollback()` on uncaught exception.
6. **MINOR (Y-target deferral inconsistency):** A9 vs. A1 — 11.O blocks on 11.N.2c but Y is queued for Rev-5.4; resolve by either blocking 11.O on Rev-5.4 too, or admitting placeholder Y.
7. **MINOR (user-filter sanity check):** add `n_distinct_traders >= 5 per week` post-filter check; many weeks show 1-3 unique traders even after bot filter.

---

**Path:** `/home/jmsbpp/apps/ThetaSwap/thetaSwap-core-dev/.worktree/ranFromAngstrom/contracts/.scratch/2026-04-24-plan-rev5.3-consolidated-review-reality-checker.md`

Prior 2 BLOCKers + HALT-verify all landed correctly; the consolidated draft introduces a new threshold-internal-inconsistency BLOCKer (N_min=80 + power_min=0.80 + MDES=0.20 SD are mutually unsatisfiable per scipy verification, power@n=80 ≈ 0.42) and an empirical dead-branch confirmation (COPM-only has 44 non-zero weeks vs N_min=80, calibration deterministically picks basket-aggregate). Mento-side basket symbols are stale (rebrand to USDm/EURm/BRLm/KESm not propagated); contract addresses verified canonical via Dune `tokens.erc20` lookup.
