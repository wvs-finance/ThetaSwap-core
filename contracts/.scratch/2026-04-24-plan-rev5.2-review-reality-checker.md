# Rev-5.2 Plan Review — Reality Checker

**Target:** `contracts/docs/superpowers/plans/2026-04-20-remittance-surprise-implementation.md` @ `b745e4e22`
**Diff:** `8c984ab56..b745e4e22` (Task 11.N.1 insert)
**Date:** 2026-04-24 16:33 ET — independent of CR / PM.

## 1. Verdict

**NEEDS WORK** — 2 BLOCKs, 3 FLAGs, 2 NITs. The on-chain data path is empirically viable (Forno + Ankr both live; `eth_getLogs` 10k-block windows return cleanly; COPM bytecode confirmed; Transfer-topic hash verified; Rev-4 decision-hash preserved byte-exact). Two schema-contract BLOCKs will cause Step-5 runtime failure as written.

## 2. Empirical-verification table (RC-P1…P13)

| ID | Claim | Evidence | Status |
|----|-------|----------|--------|
| P1 | Forno live, accepts `eth_getLogs` | head = block `0x3e24b29` (65,093,929); 9,320-block COPM window → 55 logs, no error, sub-second | PASS |
| P2 | Ankr fallback live | head = `0x3e24b2b` (±2 blocks of Forno) | PASS |
| P3 | Alchemy Celo-mainnet URL valid | `https://celo-mainnet.g.alchemy.com/v2/demo` → HTTP 429 (URL shape valid; demo key rate-limited). Free-tier 300M CU claim NOT dated in plan | FLAG F3 |
| P4 | COPM `0xc92e…5606` live + non-zero bytecode | `eth_getCode` → 400+ B EIP-1967 proxy (`0x60806040…360894a13ba1a3210667c828492db98dca3e2076…`) | PASS |
| P5 | Transfer topic hash | `Crypto.Hash.keccak(256)("Transfer(address,address,uint256)")` = `0xddf252ad1be2c89b69c2b068fc378daa952ba7f163c4a11628f55a4df523b3ef` → matches plan | PASS |
| P6 | 110,069-row Dune target | `contracts/data/copm_per_tx/README.md:12` — `Transfers … 7369028 … 01KPZVETGZGP4KDSTGKDNH1FY7 … 110,069 … 0.017` | PASS |
| P7 | Block-range budget ~10M | 65.09M head − Sept-2024 deploy ≈ 10.6M blocks / 10k window → ~1,060 calls (feasible) | PASS |
| P8 | Step-5 idempotency | `econ_schema.py:367-374`: single-col PK `week_start` + `CHECK (proxy_kind = 'net_primary_issuance_usd')`. New value CHECK-rejected; PK cannot hold two rows/week | **BLOCK B1** |
| P9 | Real-data-over-mocks compat | Step 1(e) "mock-free: hit real RPC"; non-negotiable rule mirrors global `feedback_real_data_over_mocks.md` | PASS |
| P10 | Rev-4 decision-hash | `nb1_panel_fingerprint.json:23` = `"6a5f9d1b05c18defd8b30c4b3cef6af896d6e45a2a26c1c60aa342da0a5a443c"` byte-exact | PASS |
| P11 | `web3.py` in venv | venv has `eth-hash 0.7.1`, `eth-typing 5.2.1`, `eth-utils 5.3.1`, `requests 2.32.5`; **`import web3` → `ModuleNotFoundError`** | **BLOCK B2** |
| P12 | Forno 10k-block rate tolerance | 9k-block window 55 logs OK; 163k-block window → `{"code":-32011,"message":"no backend is currently healthy"}`. 10k safe, 160k+ NOT | PASS-with-caveat |
| P13 | Primary-vs-fallback reconciliation | Plan silent on row-level disagreement between Forno and Alchemy | FLAG F1 |

## 3. Live-endpoint smoke-test output (truncated)

```
POST forno.celo.org  eth_blockNumber         → {"result":"0x3e24b29"}          (65,093,929)
POST rpc.ankr.com/celo eth_blockNumber       → {"result":"0x3e24b2b"}          (±2 blocks)
POST forno.celo.org  eth_getLogs 10k-win      → 55 COPM Transfer logs, no error (sub-second)
POST forno.celo.org  eth_getLogs 163k-win     → error -32011 "no backend … healthy"
POST forno.celo.org  eth_getCode COPM         → 0x60806040…360894a13ba1…  (EIP-1967 proxy, ~400 B)
POST celo-mainnet.g.alchemy.com/v2/demo       → HTTP 429 (URL valid; demo rate-limited)
Python keccak256("Transfer(address,address,uint256)")
                                              → 0xddf252ad1be2c89b69c2b068fc378daa952ba7f163c4a11628f55a4df523b3ef
```

## 4. Dependency gaps

- **web3.py MISSING** (B2). Plan Step 2 says "using `web3.py` or equivalent." Either (i) add `web3` to `contracts/requirements.txt` as a Step-0 prereq, or (ii) pin Step 2 to `requests`-only JSON-RPC and drop the `web3.py` wording. Smoke tests above used `curl` / plain JSON-RPC — no web3 needed for MVP.
- **pycryptodome** (or `eth_utils.keccak`) needed in tests for off-chain Transfer-topic sanity check; not in plan's implicit dep list (eth_utils IS installed → path 2 works).
- **`ALCHEMY_API_KEY`** env-var: plan says "halt + escalate if missing." Fine; but Step 1(e) smoke-test uses primary RPC only → Alchemy fallback path is never CI-exercised (F2).

## 5. Blocking findings

- **B1 — `onchain_xd_weekly` schema rejects Rev-5.2 design.** `econ_schema.py:367-374` pins `proxy_kind` to `'net_primary_issuance_usd'` via CHECK and uses a single-col PK. Step 5 will fail at runtime. Co-pinned: `econ_query_api.py:1151`, `copm_xd_filter.py:34,71,111`, `test_copm_xd_filter.py:239`. Fix: add migration to Task 11.N.1 — composite PK `(week_start, proxy_kind)` + relaxed CHECK (`IN ('net_primary_issuance_usd','b2b_to_b2c_net_flow_usd')`); update the four dependent lines; reframe "additive-only" as "data additive; schema migrated-with-test-coverage." Plan's "no code changes required" for `copm_xd_filter.py` is FALSE for the `PROXY_KIND` constant at line 71.
- **B2 — `web3.py` not installed.** See §4.

## 6. Non-blocking flags

- **F1 — Primary-vs-fallback reconciliation missing.** If Forno returns 110,065 and Alchemy 110,069, which wins? Plan covers primary-vs-Dune but not primary-vs-fallback. Add: "if both pulled, canonicalize by `(block_number, log_index)`; union minus duplicates; record source-of-record per row."
- **F2 — Alchemy fallback path untested at smoke time.** Add Step 1(f): run `fetch_copm_transfers_alchemy()` against a 1000-transfer window when `ALCHEMY_API_KEY` is set; skip-with-reason otherwise.
- **F3 — Alchemy 300M CU/month claim undated.** Cite `https://www.alchemy.com/pricing` with a retrieval date before relying on the figure.

## 7. Nits

- **N1 — COPM deployment block unspecified.** Plan says "from COPM contract deployment … per docstring" — add the exact block number to Step 2 so the implementer does not re-derive it.
- **N2 — Repo bloat.** In-tree `copm_transfers.csv` = 2,934 B / 10 rows today → ~30–50 MB after backfill. Gitignore or use git-lfs.

---

**Summary:** primary on-chain data path empirically works; two schema-contract BLOCKs + three reconciliation/dep FLAGs must close before dispatch. Default verdict NEEDS WORK.
